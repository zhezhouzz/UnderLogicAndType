# Result-First Arrow/Wand Denotation Plan

## Goal

Change `CTArrow` and `CTWand` denotation so function-typed terms are handled
like `CTSum`: first bind each produced function value with a result graph, then
prove the function-value specification for `ret f`.

This is a semantic change, not a proof-script workaround.  It prevents the TLet
Arrow/Wand proof from trying to commute the let-result extension through a
fresh argument world, which is false when the result of `e1` and the argument
resource are correlated.

## Definition Shape

Introduce value-level helpers in `Denotation/theories/TypeDenote.v`:

```coq
arrow_value_denote_gas gas Σ τx τr ef
wand_value_denote_gas gas Σ τx τr ef
```

`ty_denote_gas` uses an outer `FForall` result binder for Arrow/Wand:

```coq
FForall
  (FImpl
    (expr_result_formula_at (lvars_shift_from 0 (dom Σg))
      (tm_shift 0 e) (LVBound 0))
    (arrow_value_denote_gas gas' Σf
      (cty_shift 0 τx) (cty_shift 1 τr) (tret (vbvar 0))))
```

and analogously for `wand_value_denote_gas`.  After opening both binders, the
function value is `vbvar 1`, the argument is `vbvar 0`, and the application is:

```coq
tapp_tm (tret (vbvar 1)) (vbvar 0)
```

## Proof Recovery Order

1. Repair `TypeDenote.v`: open-env, singleton-open, env-agree, lvars/fv/scope.
2. Repair value-intro/value-elim helpers for Lam/LamD/App/AppD/Fix.
3. Repair `TypeEquivTLet.v`: Arrow/Wand use outer result correspondence first,
   then recurse only on the value-level result body.
4. Restore `tlet_intro_denotation` as an iff.

## Current Status

Completed:

- `TypeDenote.v` compiles with result-first Arrow/Wand.
- `TypeEquivCore.v` saturation handles `arrow_value_denote_gas_with` and
  `wand_value_denote_gas_with`.
- `TypeEquivTLet.v` forward transport is structural except for Arrow/Wand
  value-body obligations.
- `TypeEquivTLet.v` reverse transport is no longer one large admit: it is a
  gas induction with only Arrow/Wand value-body reverse obligations remaining.
- `Soundness.vo` and full `make TIMED=1` compile under the new definitions.
- `TypeEquivFiberTransport.v` now has proved function-position result-alias
  operational helpers:

```coq
tm_equiv_ret_value_result_alias
tm_total_equiv_ret_value_result_alias
typed_total_equiv_ret_value_result_alias
tm_equiv_tapp_value_fun_result_alias
tm_total_equiv_tapp_value_fun_result_alias
```

- `TypeEquiv.v` has the proved static/typed bridge:

```coq
ty_static_guard_ret_value_result_alias
typed_total_equiv_ret_value_result_alias_static
```

These are the low-level facts needed when an outer result graph says a fresh
function slot `f` contains the same function value as `ret v`; they now cover
both raw operational equality and the `typed_total_equiv_on` package needed by
the old direct transport theorem.

Current result-first obligations:

```coq
ty_denote_gas_tm_equiv_arrow_body
ty_denote_gas_tm_equiv_wand_body
tlet_intro_denotation_arrow_body
tlet_intro_denotation_wand_body
tlet_intro_denotation_arrow_body_reverse
tlet_intro_denotation_wand_body_reverse
fundamental_lam_case
fundamental_lamd_case
app_arrow_open_result_from_fresh_drop
fundamental_appd_case
fundamental_fix_case
fix_body_arrow_apply_self
fix_self_rec_call_reducible_measure_step
```

Status update:

- `app_arrow_open_result_from_fresh_drop` is now `Qed` in
  `ContextTyping/theories/SoundnessApp.v`.
- `fundamental_appd_case` is the next direct Soundness obligation.  The proof
  should not try to use the old Wand body directly.  It should:
  1. split `CtxStar Γ1 Γ2` into closure world `m1` and argument world `m2`;
  2. restrict the closure side to `m1 \ {x}` before opening the result-first
     function slot, exactly as ordinary App deletes `x` from the function
     world;
  3. use `res_restrict m2 {x}` as the `FBWand` argument world;
  4. frame the fresh function-result extension through the product by
     `res_extend_by_product_frame_l/r`;
  5. apply `res_models_fbwand_open_one_named_fresh`;
  6. use `ty_denote_gas_tapp_fun_result_alias_back_from_static` to replace
     `ret z` by `ret v1`;
  7. drop the fresh `z` world by minimality and reuse
     `appd_wand_result_to_arrow_open_result` followed by
     `app_arrow_result_to_target`.

Still-existing non-result-first admits:

```coq
ty_denote_gas_result_alias_at
```

and the separate concrete primitive-op-context implementation admits in
`PrimOpConcreteContext.v`.

## Next Lemmas

The next useful shared lemma should express value-denotation transport between
two worlds produced by an outer result correspondence:

```coq
arrow_value_denote_result_projection
wand_value_denote_result_projection
```

Intended shape:

- source and target worlds agree on the projection containing
  `FV(τx -> τr)` plus the fresh function result slot;
- source and target environments are both produced from the same ambient env
  by `relevant_env` plus the same function-slot binding;
- the function term is `ret f` on both sides;
- then `arrow_value_denote_gas_with` / `wand_value_denote_gas_with` transports
  by rebuilding the inner `FForall`/`FBWand`, using env-agree for the argument
  and result denotations.

For direct value cases (`Lam`, `App`, `Fix`) the operational bridge is:

```coq
expr_result_formula (tret v) f
  -> tm_equiv_on/total_equiv_on (tapp_tm (tret f) y) (tapp_tm (tret v) y)
```

which is now proved in `TypeEquivFiberTransport.v`.  If the proof needs the
function term itself rather than an application, use the corresponding
`tm_equiv_ret_value_result_alias` / `typed_total_equiv_ret_value_result_alias`
bridge.

## Acceptance Checks

```sh
timeout 50s make TIMED=1 Denotation/theories/TypeDenote.vo
timeout 50s make TIMED=1 Denotation/theories/TypeEquivTLet.vo
timeout 50s make TIMED=1 Denotation/theories/TypeEquiv.vo
timeout 50s make TIMED=1 ContextTyping/theories/Soundness.vo
timeout 180s make TIMED=1
```

Expected remaining admits are only unrelated concrete primop-context admits and
the currently known result-alias admit if it remains outside the TLet path.
