# Open / Relevant Normalization Audit

Status: active.  This file tracks remaining proof-facing wrappers and raw-open
forms in `Denotation`, `ContextTyping`, and `ContextBasicDenotation`.

Policy:

- `relevant_lvars` / `relevant_env` should be parsing abbreviations, not
  proof-facing definitions.
- Ordinary Denotation/Soundness helper statements should use direct support
  forms such as `context_ty_lvars τ ∪ tm_lvars e` and
  `lty_env_restrict_lvars Σ (...)`.
- Raw `formula_open`, `lty_env_open_one (typed_lty_env_bind ...)`,
  `cty_open (cty_shift ...)`, and unnormalized `qual_open_atom` may remain in
  syntax/normalizer lemmas, but should not leak into high-level helper
  premises.

## Initial Scan

Command:

```sh
rg -n "\brelevant_env\b|\brelevant_lvars\b" Denotation ContextTyping ContextBasicDenotation -g '*.v' \
  | cut -d: -f1 | sort | uniq -c | sort -nr
```

Largest buckets:

- `ContextBasicDenotation/theories/RelevantEnv.v`: 226
- `Denotation/theories/TypeEquivTLet.v`: 151
- `Denotation/theories/TypeDenote.v`: 121
- `Denotation/theories/TypeEquivArrowResultFirst.v`: 84
- `Denotation/theories/TypeEquivWandResultFirst.v`: 74
- `Denotation/theories/TypeEquivWand.v`: 65
- `ContextTyping/theories/SoundnessMatch.v`: 56
- `Denotation/theories/TypeEquivArrow.v`: 47
- `Denotation/theories/TypePersistWandReverse.v`: 44
- `ContextTyping/theories/SoundnessApp.v`: 42
- `ContextTyping/theories/PrimOpConcreteContext.v`: 42
- `Denotation/theories/TypeEquivFiberBody.v`: 41
- `Denotation/theories/TypePersistWandForward.v`: 40
- `ContextTyping/theories/SoundnessFixSelf.v`: 39
- `ContextTyping/theories/SoundnessAppD.v`: 36

Raw-open scan:

- `ContextBasicDenotation/theories/RelevantEnv.v`: 20
- `Denotation/theories/TypeEquivFiberTransport.v`: 14
- `ContextBasicDenotation/theories/BasicTypingFormula.v`: 13
- `ContextBasicDenotation/theories/Qualifier.v`: 11
- `Denotation/theories/TypeEquivTLet.v`: 10
- `Denotation/theories/TypeDenote.v`: 10
- `ContextBasicDenotation/theories/BasicTypingOpen.v`: 6
- `Denotation/theories/TypeEquivFiberBody.v`: 5

## Iteration 1: Source Relevant Definitions

Problem:

- `RelevantEnv.v` still defines `relevant_lvars` and `relevant_env` as real
  constants.
- This lets upper-layer helper statements keep wrapper names and encourages
  proofs to use `unfold relevant_lvars` / `unfold relevant_env`.

Action:

- Convert `relevant_lvars` and `relevant_env` to parsing abbreviations.
- Replace local `unfold relevant_*` proof steps with direct unfolding of
  `lty_env_restrict_lvars` plus support normalization.

Status: deferred after inspection.

Attempting to turn the source definitions into notations immediately exposes a
large amount of existing proof script that still performs `unfold relevant_*`.
This should be done, but not with a single mechanical replacement.  Keep this
as a source-level goal and first reduce the largest upper-layer public/helper
statements so the eventual source change is smaller.

## Iteration 2: Result-First Body Support

Problem:

- `TypeEquivArrowResultFirst.v` and `TypeEquivWandResultFirst.v` each had a
  local `*_result_body_relevant_subset` lemma whose statement exposed
  `relevant_lvars` even though its callee `result_first_result_body_relevant_subset`
  had already been normalized.

Action:

- Replaced those two local statements with direct support:
  `context_ty_lvars (...) ∪ tm_lvars (...)`.

Validation:

- `timeout 20s make TIMED=1 Denotation/theories/TypeEquivArrowResultFirst.vo`
- `timeout 20s make TIMED=1 Denotation/theories/TypeEquivWandResultFirst.vo`

Status: completed.

## Iteration 3: Env-Agree Observation Set

Problem:

- `ty_denote_gas_env_agree_on` still exposed
  `relevant_lvars τ e ⊆ X`.
- Arrow/Wand result-first env-agree helpers passed
  `relevant_lvars τbody ebody` and then immediately unfolded it.

Action:

- Changed `ty_denote_gas_env_agree_on` and
  `res_models_ty_denote_gas_env_agree_on` to require direct support:
  `context_ty_lvars τ ∪ tm_lvars e ⊆ X`.
- Updated Arrow/Wand result-first env-agree call sites to pass
  `context_ty_lvars τbody ∪ tm_lvars ebody`.

Validation:

- `timeout 20s make TIMED=1 Denotation/theories/TypeDenote.vo`
- `timeout 50s make TIMED=1 Denotation/theories/TypeEquivArrowResultFirst.vo`
- `timeout 20s make TIMED=1 Denotation/theories/TypeEquivWandResultFirst.vo`

Count movement:

- `TypeDenote.v`: 121 -> 115 relevant wrapper hits.
- `TypeEquivArrowResultFirst.v`: 84 -> 76 relevant wrapper hits.
- `TypeEquivWandResultFirst.v`: 74 -> 66 relevant wrapper hits.

Status: completed.

## Next Batches

1. `TypeDenote.v`: normalize public support lemmas and open-env lemmas first.
2. `TypeEquivArrowResultFirst.v` / `TypeEquivWandResultFirst.v`: consume the
   normalized TypeDenote boundary lemmas.
3. `TypeEquivTLet.v`: rewrite the local opened argument/result helper
   statements; it is currently the largest Denotation consumer.
4. `SoundnessApp.v` / `SoundnessAppD.v` / `SoundnessMatch.v`: normalize at the
   first opening point rather than in downstream helper lemmas.

## Iteration 4: TypeDenote Open-Env Freshness

Problem:

- `formula_open_env_denot_guard`,
  `relevant_env_result_domain_open_lift`,
  `formula_open_env_ty_denote_gas_zero`, and
  `formula_open_env_ty_denote_gas` exposed
  `dom Σ ∪ relevant_lvars τ e` in their freshness premises.

Action:

- Replaced those premises with direct support:
  `dom Σ ∪ (context_ty_lvars τ ∪ tm_lvars e)`.
- Removed matching proof-local `unfold relevant_lvars in *` steps.

Validation:

- `timeout 20s make TIMED=1 Denotation/theories/TypeDenote.vo`
- `timeout 180s make TIMED=1`

Count movement:

- `TypeDenote.v`: 115 -> 107 relevant wrapper hits.

Status: completed.

## Iteration 5: TLet Open-Arg Env-Agree Call Sites

Problem:

- `tlet_arrow_open_arg_env` and `tlet_wand_open_arg_env` still called
  `ty_denote_gas_env_agree_on` with `relevant_lvars τa ea`.

Action:

- Replaced those four call-site observation sets with
  `context_ty_lvars τa ∪ tm_lvars ea`.

Validation:

- `timeout 20s make TIMED=1 Denotation/theories/TypeEquivTLet.vo`

Status: completed.

## Iteration 6: TypeDenote Lift-Support Lemmas

Problem:

- Sum/Arrow/Wand/Persist lift-support helper statements still used
  `relevant_lvars` in their support set conclusions.

Action:

- Replaced those statements with direct
  `context_ty_lvars ... ∪ tm_lvars ...` support.

Validation:

- `timeout 20s make TIMED=1 Denotation/theories/TypeDenote.vo`
- `timeout 180s make TIMED=1`

Count movement:

- `TypeDenote.v`: 107 -> 95 relevant wrapper hits.

Status: completed.
