Soundness case bridge implementation plan
=========================================

Principle
---------

The Fundamental proof should look like the proven TLet branch:

1. The Soundness case consumes context denotation and induction hypotheses.
2. It derives concrete `ty_denote_gas` premises for the current world.
3. It calls a Denotation-level helper such as `lam_intro_denotation`.

Denotation-level helpers should not mention `ctx_denote_under`, `Γ`, or typing
induction hypotheses.  They may still mention basic typing and operational
premises when those are genuinely part of the semantic case.

Case-by-case plan
-----------------

1. AppOp

- Already has a direct helper, `appop_intro_denotation`, whose proof is complete.
- Keep the Soundness bridge proved by extracting the argument IH and calling
  `appop_intro_denotation`.

2. Lam

- Soundness bridge:
  - pick/receive the fresh parameter from the Arrow forall proof obligation;
  - build the context denotation for `CtxComma Γ (CtxBind y τx)`;
  - call the body IH to get
    `ty_denote_gas ... (<[LVFree y := erase_ty τx]> Σ) ({0 ~> y} τ) (e ^^ y)`;
  - call `lam_intro_denotation`.
- Needed bridge lemma:
  `ctx_denote_under_comma_bind_from_arg_denotation`.

3. LamD

- Same as Lam, but the body context is `CtxStar Γ (CtxBind y τx)`.
- Need a separating bridge:
  `ctx_denote_under_star_bind_from_arg_denotation`.
- This will use existing `FStar`/resource product lemmas.

4. App

- Extract function and argument IHs on the same world.
- Use the Arrow denotation's forall witness and transport the fresh witness
  back to the actual argument variable.
- Needed helper family:
  - argument fvar-to-witness transport;
  - result open/rename transport.

5. AppD

- Split the `CtxStar Γ1 Γ2` context into function and argument resources.
- Use function IH on the function resource and argument IH on the argument
  resource.
- Eliminate the Wand denotation and transport the fresh witness as in App.

6. LetD

- Split `CtxStar Γ1 Γ2`.
- Use IH1 to build the result extension for `e1`.
- Combine the result-extension world with the Γ2 resource to satisfy the body
  context `CtxStar Γ2 (CtxBind x τ1)`.
- Call `letd_intro_denotation`.

7. Fix

- Denotation helper should follow HATs' `denotation_application_fixed` shape.
- The body premise is not merely a recursive IH; it says that after opening the
  ordinary argument `y`, the opened body denotes a function expecting recursive
  self:

    `CTArrow (fix_rec_call_ty b y τx τ) ({0 ~> y} τ)`

- The Denotation proof will:
  - use `constant_lt_for_base_well_founded b`;
  - perform well-founded induction on the current base constant;
  - build the recursive self denotation from the induction hypothesis.

8. FixD

- Same HATs-shaped recursive helper as Fix.
- The Soundness bridge builds the body context with `CtxStar`.

9. MatchBoth

- Split the `CtxSum` context via `FPlus`.
- Call the two scrutinee IHs and the two branch IHs on their corresponding
  branch worlds.
- Call `match_both_intro_denotation`.

10. MatchTrue / MatchFalse

- The Denotation-level helper must receive an unreachable premise.
- The Soundness bridge turns typing-level `branch_unreachable` into a current
  world formula:

    `m ⊨ FImpl (ty_denote_gas (cty_depth (bool_precise_ty b)) Σ
          (bool_precise_ty b) (tret v)) FFalse`

- Then it calls `match_true_intro_denotation` or
  `match_false_intro_denotation`.

Implementation order
--------------------

0. Redesign context denotation so every recursive call first fixes the ambient
   erased context as a fiber:

   `ctx_denote_under Σ Γ :=
      FFibVars (lvars_of_atoms (dom Σ))
        (FAnd (basic_world_formula (atom_env_to_lty_env (ctx_erasure_under Σ Γ)))
          (match Γ with ... end))`.

   This keeps the denotation entirely inside Context Logic while making the
   intended invariant explicit: every `dom Σ` fiber of a world satisfying
   `ctx_denote_under Σ Γ` must satisfy the context body after substituting that
   fiber projection.  It does *not* imply that the ambient world itself has a
   singleton projection on `dom Σ`.  The recursive call for
   `CtxComma Γ1 Γ2` is simply

     `ctx_denote_under (Σ ∪ erase_ctx Γ1) Γ2`

   because that call itself adds the fiber for the enlarged ambient context.
   No extra local `FFibVars (dom Γ1)` wrapper is needed.

   The plain context denotation should become:

     `ctx_denote Γ := ctx_denote_under ∅ Γ`.

0a. Add/restore the Logic/Algebra support needed by the new outer fiber.

   Expected helper families:

   - Fiber projection singleton:
     `res_fiber_from_projection ... -> res_restrict mfib X = singleton_world σ`.
     Current concrete wrappers:
       `res_restrict_fiber_from_projection_dom_singleton`,
       `res_restrict_fiber_from_projection_eq_singleton`.
     The empty special case is also available:
       `res_fiber_from_projection_empty_self`,
       `res_fiber_from_projection_empty_eq`.
   - Fiber nesting/idempotence:
     consecutive fibers should be represented as one fiber over the union of
     the two domains.  Do not avoid overlap by projecting the second step over
     `Y ∖ X`; prove the overlap-aware version instead.
     Current Logic-facing obligation:
       `res_models_FFibVars_nested_union_iff`:
         `m ⊨ FFibVars D1 (FFibVars D2 φ) <->
          m ⊨ FFibVars (D1 ∪ D2) φ`.
     This is now the primary theorem.  Prove it by unfolding the `FFibVars`
     model rule, composing the two projection stores, and transporting
     `formula_msubst_store` through the composed projection.  If a direct proof
     needs an algebra fact, keep that fact about consecutive fiber projections
     in Algebra and expose only this Logic theorem to Denotation/Typing.
     The implementation is split into the two reviewable directions
     `res_models_FFibVars_nested_union_l` and
     `res_models_FFibVars_nested_union_r`; the iff lemma is just the pair of
     those two directions.
     The algebra projection support currently has the same shape, but with
     overlap handled by compatible/restricted projection stores:
       `res_fiber_from_projection_nested_union_l`
       `res_fiber_from_projection_nested_union_r`
     Expected forward shape:
       `res_fiber_from_projection m X σX mfibX ->
        res_fiber_from_projection mfibX Y σY mfibXY ->
        storeA_compat σX σY ->
        res_fiber_from_projection m (X ∪ Y) (σX ∪ σY) mfibXY`.
     The compatibility premise is derivable for actual consecutive
     projections by the proved helper:
       `res_fiber_from_projection_store_compat`
     (abstract version `resA_fiber_from_projection_store_compat`).
     Current state: both overlap-aware directions
       `resA_fiber_from_projection_nested_union_l`
       `resA_fiber_from_projection_nested_union_r`
     are proved at the abstract Algebra level; the concrete `res_*` versions
     are thin wrappers.
     Expected reverse shape:
       `res_fiber_from_projection m (X ∪ Y) σXY mfibXY ->
        exists σX mfibX σY,
          σX = storeA_restrict σXY X /\
          σY = storeA_restrict σXY Y /\
          res_fiber_from_projection m X σX mfibX /\
          res_fiber_from_projection mfibX Y σY mfibXY`.
     In the Logic proof, the second projection domain will often be the
     residual domain generated by `formula_msubst_store_fibvars`, e.g.
     `lvars_fv (D2 ∖ lvars_of_atoms (dom σX))`, because the outer
     substitution has already fixed the overlapping part.  This is still an
     instance of the general overlap-aware algebra lemma; the algebra statement
     itself should not bake in a difference.
     These are deliberately about projection composition only; formula-level
     substitution composition stays in `FormulaSyntax`.
     The small proved support lemma
       `res_fiber_from_projection_world_dom`
     records that taking a fiber does not change the ambient world domain; use
     it for scoped/fv obligations after moving from `m` to `mfib`.
     Current syntax-side composition lemma:
       `formula_msubst_store_union`:
         compatible store domains imply
         `formula_msubst_store σ2 (formula_msubst_store σ1 φ) =
          formula_msubst_store (σ1 ∪ σ2) φ`.
     This formula lemma is proved by structural induction, with the only
     remaining primitive obligation being qualifier-level composition:
       `lqual_mlsubst_union`.
     Strengthen this qualifier lemma with an overlap-compatible premise:
       `storeA_compat ρ1 ρ2 ->
        lqual_mlsubst ρ2 (lqual_mlsubst ρ1 q) =
        lqual_mlsubst (ρ1 ∪ ρ2) q`.
     Try exact equality first via `logic_qualifier_ext`; only fall back to a
     models-level/denote transport if dependent `LWorldOn` paths become
     genuinely unmanageable.
     Current state: the exact equality wrapper is proved.  The remaining named
     primitive is the expected path equation for the two back-construction
     routes:
       `lworld_on_mlsubst_back_union`.
     This is a private proof helper for `lqual_mlsubst_union`, not a public
     interface.  Its domain mentions `(D ∖ dom ρ1)` only because the type of
     `lworld_on_mlsubst_back` forces that intermediate domain when composing
     two back substitutions.  Do not generalize it unless another proof needs
     the same construction; public clients should use only `lqual_mlsubst_union`.
     The old subset-shaped `res_models_FFibVars_nested_elim` must remain only a
     thin corollary using union plus same-fv transport for `FFibVars`.
     Current same-fv transport:
       `res_models_FFibVars_same_fv`:
         `lc_lvars D2 ->
          lvars_fv D1 = lvars_fv D2 ->
          m ⊨ FFibVars D1 φ -> m ⊨ FFibVars D2 φ`.
     The explicit `lc_lvars D2` premise is necessary because equal free-variable
     sets do not rule out extra bound lvars in `D2`.
     The same helper now gives the proved wrapper:
       `res_models_FFibVars_idemp_elim`.
   - Singleton fiber connective splitting:
     the old unconditional distributivity obligations
       `res_models_FFibVars_star_distrib_l`
       `res_models_FFibVars_plus_distrib_l`
     are too strong after `FFibVars` started substituting the chosen projection
     store into the body.  `FFibVars D (FStar φ ψ)` only says that every
     `D`-projection fiber can be split; if the ambient world has more than one
     possible `D` projection, the splits for different fibers need not assemble
     into one global split of `m`.

     Replace those obligations by singleton-projection versions.  First add
     Algebra/Logic helpers saying that a singleton projection makes the
     `FFibVars` witness unique:

       `res_fiber_singleton_projection_inv`:
         `dom σD = X ->
          res_restrict m X = singleton_world σD ->
          res_fiber_from_projection m X σ mfib ->
          σ = σD /\ mfib = m`.

       `res_models_FFibVars_singleton_elim`:
         `dom σD = lvars_fv D ->
          res_restrict m (lvars_fv D) = singleton_world σD ->
          m ⊨ FFibVars D φ ->
          m ⊨ formula_msubst_store σD φ`.

       `res_models_FFibVars_singleton_intro`:
         `lc_lvars D ->
          dom σD = lvars_fv D ->
          res_restrict m (lvars_fv D) = singleton_world σD ->
          m ⊨ formula_msubst_store σD φ ->
          m ⊨ FFibVars D φ`.

     The first useful algebra helpers are product/sum inheritance helpers.
     These are proved and remain useful when the branch worlds already contain
     the fixed domain:

       `res_product_le_singleton_restrict_inv`:
         `res_product m1 m2 Hc ⊑ m ->
          X ⊆ world_dom (m1 : WorldT) ->
          X ⊆ world_dom (m2 : WorldT) ->
          res_restrict m X = singleton_world σ ->
          res_restrict m1 X = singleton_world σ /\
          res_restrict m2 X = singleton_world σ`.

       `res_sum_le_singleton_restrict_inv`:
         same shape for `res_sum`.

     However, this is not enough for the shared eliminator after `mlsubst`.
     The reason is subtle but important: substituting the singleton store into
     `FFibVars D φ` rewrites it to a residual fiber over
     `D ∖ lvars_of_atoms (dom σD)`, whose free-variable projection is empty
     when `dom σD = lvars_fv D`.  Therefore the star/plus split of
     `formula_msubst_store σD (FFibVars D φ)` can return components whose
     worlds do not contain `lvars_fv D` at all.  We cannot simply invert
     singleton projection on those components.

     The next required algebra fact is a singleton pullback/lift operation:
     if `m` has a singleton projection `σD` on `X`, and a component `mi`
     produced by a product/sum split is below `m`, construct an enlarged
     component `miD` that adds exactly this singleton `X` projection while
     remaining below `m`.  The two enlarged components should still be
     compatible/summable because they share the same singleton overlap.

      `res_product_le_singleton_pullback` (planned):
         from `res_product m1 m2 Hc ⊑ m` and
         `res_restrict m X = singleton_world σX`, construct `m1D`, `m2D`,
         `HcD` such that
           `res_product m1D m2D HcD ⊑ m`,
           `res_restrict m1D X = singleton_world σX`,
           `res_restrict m2D X = singleton_world σX`,
           and `m1D/m2D` agree with `m1/m2` on the old branch formula support.

        Use the concrete construction

          `m1D := res_restrict m (world_dom (m1 : WorldT) ∪ X)`
          `m2D := res_restrict m (world_dom (m2 : WorldT) ∪ X)`.

        This construction is chosen because both enlarged worlds are directly
        obtained from the ambient `m`, so the final `⊑ m` proof is a restrict
        proof rather than a reconstruction of arbitrary ambient stores.  It
        should provide:

          `res_restrict m1D (world_dom (m1 : WorldT)) = m1`
          `res_restrict m2D (world_dom (m2 : WorldT)) = m2`

        using `m1 ⊑ res_product m1 m2 Hc ⊑ m` and the corresponding right
        component fact, and:

          `res_restrict m1D X = singleton_world σX`
          `res_restrict m2D X = singleton_world σX`

        by commuting nested restrictions with
        `res_restrict m X = singleton_world σX`.

        Compatibility of `m1D` and `m2D` is the only real algebraic point.
        Their overlap is
        `(world_dom m1 ∪ X) ∩ (world_dom m2 ∪ X)`.  Agreement on the old
        `world_dom m1 ∩ world_dom m2` part follows from the original
        `world_compat m1 m2` after restricting `m` back to `m1`/`m2`; agreement
        on the `X` part follows from the singleton projection.  Package this
        either as part of the pullback proof or as a smaller helper:

          `world_compat_restrict_union_singleton_overlap`.

        After this lemma, branch model transport is:

          `n1 ⊨ θ`
          `formula_fv θ ⊆ world_dom (n1 : WorldT)`
          `res_restrict m1D (world_dom (n1 : WorldT)) = n1`
          ------------------------------------------------
          `m1D ⊨ θ`

        using `res_models_minimal_on` / `res_models_kripke`.

      `res_sum_le_singleton_pullback` (planned):
         analogous for `res_sum`.

        For sum, prefer reusing the existing
        `res_sum_lift_along_restrict`/slice-pullback infrastructure when
        possible.  If the lifted worlds become full-domain subworlds of `m`,
        derive the singleton projection by a small subset lemma:

          `res_subset_singleton_restrict`:
            `res_subset p m ->
             X ⊆ world_dom (p : WorldT) ->
             res_restrict m X = singleton_world σX ->
             res_restrict p X = singleton_world σX`.

        The product route cannot use full-domain pullback directly because two
        full-domain subworlds need not be compatible unless the whole ambient
        world is singleton; hence the narrower `world_dom ni ∪ X` construction.

     The Logic-facing replacement for the old shared eliminator is then:

       `res_models_FFibVars_star_elim_shared_singleton`:
         `dom σD = lvars_fv D ->
          res_restrict m (lvars_fv D) = singleton_world σD ->
          m ⊨ FFibVars D (FStar (FFibVars D φ) (FFibVars D ψ)) ->
          exists m1 m2 Hc,
            res_product m1 m2 Hc ⊑ m /\
            m1 ⊨ FFibVars D φ /\
            m2 ⊨ FFibVars D ψ`.

       `res_models_FFibVars_plus_elim_shared_singleton`:
         analogous for `FPlus`.

     Revised proof shape for star:
       1. use `res_models_FFibVars_singleton_elim` to turn the outer
          `FFibVars` into
          `m ⊨ formula_msubst_store σD (FStar ...)`;
       2. simplify the substitution through `FStar`;
       3. use `res_models_star_iff` to obtain `m1`, `m2`;
       4. use the product singleton pullback lemma to enlarge these components
          to `m1D`, `m2D` carrying the fixed projection;
       5. use the branch support/agreement part of the pullback lemma to
          transport the already obtained branch models from `m1/m2` to
          `m1D/m2D`;
       6. rewrite `formula_msubst_store_fibvars`, use empty/residual-fv
          normalization and `res_models_FFibVars_singleton_intro` to rebuild
          `m1D ⊨ FFibVars D φ` and `m2D ⊨ FFibVars D ψ`.
     The plus proof is the same with `res_models_plus_iff` and sum inheritance.

     Denotation should not call an unconditional
     `ctx_denote_under_star_elim`.  Replace it by a singleton-aware eliminator:

       `ctx_denote_under_star_elim_singleton`:
         `dom σΣ = dom Σ ->
          res_restrict m (dom Σ) = singleton_world σΣ ->
          m ⊨ ctx_denote_under Σ (CtxStar Γ1 Γ2) ->
          exists m1 m2 Hc,
            res_product m1 m2 Hc ⊑ m /\
            m1 ⊨ ctx_denote_under Σ Γ1 /\
            m2 ⊨ ctx_denote_under Σ Γ2`.

     The analogous `ctx_denote_under_sum_elim_singleton` handles `CtxSum`.
     Upper proofs obtain the singleton premise only after choosing a concrete
     outer `FFibVars (ctx_base_vars Σ)` fiber of `ctx_denote_under`; it is not a
     consequence of an arbitrary ambient `m ⊨ ctx_denote_under Σ Γ`.

     Current proved Denotation helpers:

       `ctx_denote_under_body` and `ctx_denote_under_unfold_body`:
         name the body under the outer `FFibVars (ctx_base_vars Σ)` wrapper.

       `ctx_denote_under_projected_body`:
         `res_fiber_from_projection m (dom Σ) σΣ mfib ->
          m ⊨ ctx_denote_under Σ Γ ->
          mfib ⊨ formula_msubst_store σΣ (ctx_denote_under_body Σ Γ)`.

       `ctx_denote_under_fiber_from_projection`:
         `res_fiber_from_projection m (dom Σ) σΣ mfib ->
          m ⊨ ctx_denote_under Σ Γ ->
          mfib ⊨ ctx_denote_under Σ Γ`.

       `ctx_denote_under_star_elim_in_fiber` / `...sum...`:
         split a chosen `dom Σ` fiber `mfib`, not the original ambient world.

     Do not try to prove either of the following false routes:

       `res_fiber_from_projection m X σ mfib -> mfib ⊑ m`
       `res_restrict m X = singleton_world σ -> m ⊨ φ ->
        m ⊨ formula_msubst_store σ φ`.

     The first is false because `⊑` is raw restriction equality; since a fiber
     has the same domain as its source, it would imply `mfib = m`.  The second
     is false because substituting the projection store changes the formula's
     interpretation.  Star/sum/appd/letd soundness must therefore be proved by
     a fiberwise bridge: choose each base fiber from `ctx_denote_under`, prove
     the corresponding denotation inside that fiber, and rebuild the required
     top-level denotation using a Denotation-specific fiberwise introduction
     lemma, not by transporting arbitrary formulas through `mlsubst`.
   - Models-level `FFibVars` connective helpers, especially for `FAnd`,
     `FStar`, and `FPlus`.  Avoid formula equality when `formula_msubst_store`
     would make equality too strong; prefer `m ⊨ ... -> ...` / iff lemmas.
     Completed helpers:
       `formula_msubst_store_empty`,
       `res_models_FFibVars_empty_iff`,
       `res_models_FFibVars_and_l`,
       `res_models_FFibVars_and_r`.
     The old unconditional star/plus distributivity lemmas have been deleted.
     The only supported route is through the singleton-projection eliminators
     above; upper proofs must expose the outer context fiber before splitting.

   The context basic-world projection has been moved to the BasicDenotation
   layer as:

     `basic_world_formula_fibvars_elim`.

   Prove easy directions immediately.  Keep hard algebra facts as named
   admitted obligations only if needed to wire the new denotation through the
   context bridges.

   During migration, delete the old compatibility alias `ctx_denote_in_env`
   entirely if it still exists.  The live context denotation should be just
   `ctx_denote_under`.

0b. Change the Fundamental theorem to the fiberwise statement.

   The old direct ambient conclusion

     `ctx_denote_under Σ Γ ⊫ ty_denote_under Σ Γ τ e`

   is not aligned with the new context denotation.  `ctx_denote_under Σ Γ`
   only gives the context body after choosing a `dom Σ` fiber and applying the
   corresponding `formula_msubst_store`.  Therefore the live theorem should be:

     `ctx_denote_under Σ Γ ⊫
        FFibVars (ctx_base_vars Σ) (ty_denote_under Σ Γ τ e)`.

   Equivalently, use the named wrapper:

     `ty_denote_under_fiber Σ Γ τ e`.

     The induction proof should unfold the goal's outer `FFibVars`, choose the
     same base projection as the context premise, and prove each case inside that
     chosen fiber.  Old direct case lemmas such as `fundamental_letd_case`,
     `fundamental_appd_case`, and `fundamental_match_both_case` are legacy
     reference points only; the new case lemmas should have fiberwise conclusions.
     At top-level corollaries with `Σ = ∅`, discharge the wrapper using
     `res_models_FFibVars_empty_iff`.

   Current proved support:

     `ctx_base_vars_lc`
     `ty_denote_under_fiber_intro`
     `ty_denote_under_fiber_elim_projection`

   The proof-facing `Fundamental` theorem now uses only the fiberwise case
   lemmas.  The next proof layer is to prove those case lemmas by applying
   `ty_denote_under_fiber_intro`, then using
   `ctx_denote_under_projected_body` to obtain the substituted context body in
   the selected fiber.

1. Keep the current `ctx_sub_under` meaning: it restricts the current world to
   the judgment support `X` and then satisfies the target context.  Prove the
   Fundamental case through one Denotation-level transport lemma:

   `ty_denote_under_ctx_sub_transport`.

   This helper packages the two facts that otherwise make the case noisy:
   denotation only observes `fv_tm e ∪ fv_cty τ`, and erased environments that
   agree on that support give the same type denotation.

2. Move context bridge lemmas out of `ContextTyping/theories/Soundness.v` and
   into the Denotation layer.  Soundness should only instantiate IHs and call
   these bridges.  The bridge family is:

   - `ctx_denote_under_comma_bind_from_arg_denotation`
   - `ctx_denote_under_star_bind_from_arg_denotation`
   - `ctx_denote_under_star_elim_singleton`
   - `ctx_denote_under_sum_elim_singleton`
   - `ty_denote_under_comma_bind_to_lvar_insert`
   - `ty_denote_under_star_bind_to_lvar_insert`
   - `ty_denote_gas_star_union_to_ctx`
   - `basic_typing_star_union_lty_env`
   - `letd_body_world_compat`

   The result-extension bridges should also live below Soundness, but their
   statements must avoid importing the typing judgment into Denotation.  They
   should be phrased in terms of semantic/basic premises, and Soundness should
   derive those premises from `context_typing_wf`.

2a. Strengthen the well-formedness side condition used by typing.

   The old `context_typing_wf` was only a packed regularity definition:

     `wf_context_ty_under Σ Γ τ /\ ctx_erasure_under Σ Γ ⊢ₑ e ⋮ erase_ty τ`.

   Keep that shape as `context_typing_regular`, but replace
   `context_typing_wf` by an inductive predicate whose type side condition is
   syntax-directed.  In particular, the `CTWand τx τr` case must record
   `basic_context_ty (dom Σ) τx`, not merely
   `basic_context_ty (dom Σ ∪ ctx_dom Γ) τx`.  This is the invariant needed by
   `CtxStar Γ (CtxBind y τx)`: a disjoint function parameter may not depend on
   the captured context.

   Prove projection lemmas immediately:

   - `context_typing_wf_regular`
   - `context_typing_wf_basic_typing`
   - `context_typing_wf_basic_context_ty_erased`
   - `context_typing_wf_wand_arg_global`

   Existing consumers should use these projections instead of destructing
   `context_typing_wf` as the old pair.

3. Prove easy erased-environment transport bridges immediately.  These are
   mostly `ctx_erasure_under` equality/agreement plus
   `atom_env_to_lty_env_insert_free`.

4. Prove `star_elim` and `sum_elim` by unfolding `ctx_denote_under`, applying
   `res_models_star_iff` / `res_models_plus_iff`, and pushing the outer
   `basic_world_formula` assumptions down with `basic_world_formula_subenv`.

5. Prove the argument-bind context bridges using a general preservation lemma:
   if a world extension or product does not remove the relevant context support,
   old context formulas remain true, and the new bind comes directly from the
   supplied argument denotation.

6. Leave genuinely semantic case lemmas in
   `Denotation/theories/DenotationCases.v` as named obligations until
   their individual case proof is tackled.  Do not replace them with old
   wrapper stacks.

7. Strengthen the direct Denotation case helpers with an explicit guard premise
   where needed.

   The robust split is:

   - Soundness derives the *static* denotation guard

       `context_ty_wf ∧ basic_world ∧ expr_basic_typing`

     from `context_typing_wf` and `ctx_denote_under`.
   - Each Denotation-level helper proves the genuinely semantic/operational
     part, especially `expr_total_formula`, for its own case.

   This avoids making App/Match helpers assume their own totality result while
   still removing hidden dependencies on the old packed `context_typing_wf`.
   For value-introduction helpers such as `lam_intro_denotation`,
   `lamd_intro_denotation`, `fix_intro_denotation`, and
   `fixd_intro_denotation`, a later cleanup may strengthen the static guard to a
   zero-gas guard once the generic `tret` totality lemma is in place.

8. Active route: prove Fundamental fiberwise, not by ambient direct denotation.

   The live theorem concludes

     `ctx_denote_under Σ Γ ⊫ ty_denote_under_fiber Σ Γ τ e`.

   For each case, the proof shape is:

   - introduce the ambient world `m` and `Hctx`;
   - apply `ty_denote_under_fiber_intro`;
   - fix an arbitrary base projection

       `res_fiber_from_projection m (dom Σ) σΣ mfib`;

   - use `ctx_denote_under_projected_body` to get the context body in that
     selected fiber:

       `mfib ⊨ formula_msubst_store σΣ (ctx_denote_under_body Σ Γ)`;

   - prove the case goal inside that same fiber:

       `mfib ⊨ formula_msubst_store σΣ
          (ty_denote_under Σ Γ τ e)`.

   This is the only sound route currently.  Do not prove or use either of the
   following false ambient shortcuts:

   - `res_restrict m X = singleton_world σ -> m ⊨ φ ->
      m ⊨ formula_msubst_store σ φ`;
   - `res_fiber_from_projection m X σ mfib -> mfib ⊑ m`.

9. First concrete proof layer for route 8.

   Add syntax-level freshness lemmas for `mlsubst`, because simple cases such
   as constants reduce to formulas whose free variables are disjoint from the
   projected base store:

   - `lqual_msubst_store_fresh`;
   - `formula_msubst_store_fresh`.

   These lemmas are intentionally freshness based, not singleton/fiber based.
   Then use them to prove the first real fiber case:

   - `fundamental_const_case_fiber`.

   After this, repeat the same pattern for `var` and `appop`: if the case only
   needs the selected fiber's context body plus a direct denotation helper,
   prove a small case-specific msubst transport lemma in the Denotation layer;
   otherwise leave a precisely named obligation and keep the Fundamental spine
   fiberwise.

   Current progress:

   - `lqual_msubst_store_fresh`, `formula_msubst_store_fresh`, and
     `formula_fv_const_precise_ty_denote_gas_empty` are proved.
   - `fundamental_const_case_fiber` is proved.
   - `fundamental_var_case_fiber` is proved directly from
     `ctx_denote_under_projected_body`; it does not use the obsolete ambient
     `ctx_denote_under_bind_inv` route.
   - The reusable scoped side-condition helper
     `ty_denote_under_fiber_scoped_from_context_typing` is proved in
     `Soundness.v`.
   - `fundamental_sub_case_fiber` and `fundamental_ctx_sub_case_fiber` now use
     the fiberwise spine, with the remaining nontrivial msubst transports named
     as:
       `sub_type_under_msubst_transport`;
       `ctx_sub_under_msubst_transport`.
   - `fundamental_appop_case_fiber` now uses the fiberwise spine and the
     Denotation-level named obligation
       `appop_intro_denotation_msubst`.
   - The obsolete ambient Soundness case block and the unused admitted ambient
     context-construction bridges in `Denotation.Context` have been deleted.
     Future context-construction lemmas should be reintroduced only in the
     fiberwise shape.

10. Revised active route: reduce the fiberwise theorem to a fibered implication.

   The new key observation is that most case proofs should not be re-proved as
   bespoke `formula_msubst_store` transports.  The common shape

     `FFibVars (ctx_base_vars Σ) (ctx_denote_under_body Σ Γ)
      ⊫
      FFibVars (ctx_base_vars Σ) p`

   should instead be handled through the Logic connective:

     `FFibVars (ctx_base_vars Σ)
        (FImpl (ctx_denote_under_body Σ Γ) p)`.

   In words: after fixing the base `dom Σ` fiber, the body of the proof is the
   old ambient implication from the context body to the target formula.  The new
   route only strengthens the proof by requiring the selected `dom Σ`
   projection to be fixed/singleton inside the chosen fiber; most Denotation
   helpers do not need to mention this fiberwise premise.

   First prove the Logic helper:

     `res_models_FFibVars_impl_elim`:
       `m ⊨ FFibVars D (FImpl φ ψ) ->
        m ⊨ FFibVars D φ ->
        m ⊨ FFibVars D ψ`.

   Proof outline:
   - take the same `σ, mfib` projection for both `FFibVars` premises;
   - unfold/normalize
       `formula_msubst_store σ (FImpl φ ψ)`
     to
       `FImpl (formula_msubst_store σ φ)
              (formula_msubst_store σ ψ)`;
   - apply `res_models_impl_elim`.

   Also add an intro/map helper if it improves the case scripts:

     `res_models_FFibVars_impl_intro`:
       prove `m ⊨ FFibVars D (FImpl φ ψ)` by proving, for every selected fiber,
       that `formula_msubst_store σ φ` implies `formula_msubst_store σ ψ`.

   After this helper, the Soundness case shape should become:

   1. Prove or obtain

        `m ⊨ FFibVars (ctx_base_vars Σ)
              (FImpl (ctx_denote_under_body Σ Γ)
                     (ty_denote_under Σ Γ τ e))`.

   2. Rewrite `Hctx : m ⊨ ctx_denote_under Σ Γ` using
      `ctx_denote_under_unfold_body`, so it has the matching

        `m ⊨ FFibVars (ctx_base_vars Σ) (ctx_denote_under_body Σ Γ)`.

   3. Apply `res_models_FFibVars_impl_elim`.

   This replaces the current pilot obligations:

     `sub_type_under_msubst_transport`;
     `ctx_sub_under_msubst_transport`;
     `appop_intro_denotation_msubst`.

   These obligations should be deleted once their cases are rewritten through
   the shared `FFibVars/FImpl` proof shape.

11. Updated case-by-case plan under the unified route.

   Const:
   - Already proved directly via freshness because the precise constant
     denotation has no atom free variables.
   - It may stay as a direct proof; no need to force it through implication.

   Var:
   - Already proved directly from `ctx_denote_under_projected_body`.
   - It may stay direct, since this is exactly the context-body projection.

   Sub:
   - Prove the fibered implication:

       `FFibVars D
          (FImpl (ctx_denote_under_body Σ Γ)
                 (ty_denote_under Σ Γ τ2 e))`.

   - Inside the selected fiber, use the old `sub_type_under` implication on the
     substituted context body.  If direct use of `sub_type_under` still expects
     `ctx_denote_under Σ Γ`, prove a small bridge from selected-fiber context
     body to the old implication premise, not a general false ambient
     substitution theorem.

   CtxSub:
   - Same outer route as Sub.
   - The old `ctx_sub_under` support/restrict part should be used inside the
     selected fiber, with agreement transport for
     `ty_denote_under`.  Keep the proof as a context/body implication;
     do not expose a case-specific msubst lemma.

   TLet:
   - Prove the fibered implication from `ctx_denote_under_body Σ Γ` to the
     `tlete` denotation.
   - Inside the selected fiber, derive totality for `e1` from the fibered IH,
     construct the result extension, use the body IH, and call
     `denot_tlet_direct_in_ctx` / `tlet_intro_denotation`.
   - The Denotation-level `tlet_intro_denotation` remains non-fiberwise.

   LetD:
   - Same route, but inside the selected fiber use
     `ctx_denote_under_star_elim_in_fiber`.
   - Feed the split branch worlds to the two IHs and call
     `letd_intro_denotation`.

   Lam:
   - Prove the fibered implication from the context body to the Arrow
     denotation.
   - In the Arrow forall body, build the context body for
     `CtxComma Γ (CtxBind y τx)` and call the body IH.
   - Use `lam_intro_denotation` unchanged.

   LamD:
   - Same as Lam, but use the separating/product context bridge for
     `CtxStar Γ (CtxBind y τx)`.
   - Use `lamd_intro_denotation` unchanged.

   App:
   - Inside the implication proof, obtain the function and argument denotations
     from the two IHs using the same outer context body.
   - Call `app_elim_denotation` unchanged.

   AppD:
   - Inside the selected base fiber, split the `CtxStar Γ1 Γ2` body with
     `ctx_denote_under_star_elim_in_fiber`.
   - Apply the two IHs to the split worlds, then call
     `appd_elim_denotation`.

   Fix/FixD:
   - Keep the Denotation helpers `fix_intro_denotation` and
     `fixd_intro_denotation` non-fiberwise.
   - The Soundness bridge only supplies the selected-fiber context body and the
     opened recursive-body IH.
   - Any well-founded recursion details remain in the Denotation helper.

   AppOp:
   - Delete `appop_intro_denotation_msubst`.
   - Prove the fibered implication using the existing complete
     `appop_intro_denotation` inside the selected fiber.
   - The primop semantic premise remains the old
     `wf_primop_semantic` entailment.

   MatchBoth:
   - Inside the selected base fiber, split `CtxSum Γt Γf` with
     `ctx_denote_under_sum_elim_in_fiber`.
   - Apply the four IHs on the branch worlds and call
     `match_both_intro_denotation`.

   MatchTrue / MatchFalse:
   - `branch_unreachable` should be used through the same
     `FFibVars/FImpl` route.
   - If necessary, introduce a fibered unreachable bridge:

       from the selected context body and `branch_unreachable`,
       derive the selected-fiber implication to `FFalse`.

   - Then call `match_true_intro_denotation` /
     `match_false_intro_denotation` unchanged.

12. Execution order for the revised plan.

   1. Prove `res_models_FFibVars_impl_elim` in `FormulaConnectives.v`.
      Add `res_models_FFibVars_impl_intro` only if it materially simplifies
      the case scripts.

   2. Rewrite the pilot cases in `Soundness.v`:
      `sub`, `ctx-sub`, `appop`.
      Delete:
        `sub_type_under_msubst_transport`;
        `ctx_sub_under_msubst_transport`;
        `appop_intro_denotation_msubst`.

   3. Recompile `ContextLogic`, `Denotation`, and `ContextTyping/Soundness`.

   4. Rewrite the additive/non-splitting cases:
      `let`, `lam`, `app`, `fix`, `match_true`, `match_false`.

   5. Rewrite the splitting cases:
      `letd`, `lamd`, `appd`, `match_both`.
      Use the existing singleton-aware star/sum fiber eliminators.

   6. Remove any remaining fiber-specific case obligations that are replaced by
      the shared implication route.

   7. Run full `make` and commit the checkpoint.

13. `formula_msubst_store` normalization and denotation transport.

   The fibered implication route should not force every existing
   Denotation-level helper to become fiber-aware.  The right bridge is to make
   the selected-fiber substitution explicit and then normalize it back to the
   ordinary denotation shape that helpers such as `tlet_intro_denotation`,
   `lam_intro_denotation`, and `appop_intro_denotation` already understand.

   Logic layer:

   - Strengthen the existing syntax Ltac for `formula_msubst_store`.
     It should normalize through the formula constructors
     (`FAnd`, `FImpl`, `FStar`, `FPlus`, `FFibVars`, `FOver`, `FUnder`,
     `FForall`) and leave atom-specific formulas to upper layers.
     The current `formula_msubst_syntax_norm` only rewrites the `FFibVars`
     case; extend it so case scripts stop writing
     `cbn [formula_msubst_store formula_mlsubst]` by hand.

   - Prove the connective-level eliminator first:

       `res_models_FFibVars_impl_elim`:
         `m ⊨ FFibVars D (FImpl φ ψ) ->
          m ⊨ FFibVars D φ ->
          m ⊨ FFibVars D ψ`.

     This is the first reusable proof step for all fiberwise Soundness cases.
     Add an intro lemma only if the later case scripts need it.

     Current progress:
       `res_models_FFibVars_impl_elim` is proved.
       `res_models_FFibVars_impl_intro` is also proved; it packages the common
       "prove the selected-fiber implication after `formula_msubst_store`
       normalization" pattern.
       `formula_msubst_syntax_norm` now normalizes all pure formula
       constructors, not just `FFibVars`.

   BasicDenotation / TypeLanguage / Denotation layer:

   - Do not define a fake `formula_msubst_store` on terms or types.  Instead
     reuse the real syntax operations:

     `lstore_instantiate_tm σ e`
     qualifier mlsubst/open support
     lty-env projection/instantiation support as needed.

   Freshness/open-env cleanup before the main transport:

   - The old `basic_tm_has_ltype_by_open_env` / `*_to_open_env` route is now a
     compatibility/spec bridge only.  It should not be used to prove
     `formula_msubst_store` transport.  Since public `basic_tm_has_ltype` is
     the inductive lvar-level relation, the old `open_env_fresh_for_lvars η`
     premises can disappear from the active msubst proof path.  Remaining
     exact `formula_open_env_*` lemmas may still need freshness, but those are
     syntax/open normalization facts, not BasicTyping obligations.

   - First normalize the fixed store to the variables a formula can see:

       `formula_msubst_store_restrict_fv_subset`:
         `formula_fv φ ⊆ X ->
          formula_msubst_store σ φ =
          formula_msubst_store (store_restrict σ X) φ`.

     This is proved in `FormulaSyntax.v` from the qualifier-level lemma
     `lqual_msubst_store_restrict_subset`.  The key low-level fact is that a
     lifted atom store restricted to a lvar domain `D` only depends on
     `lvars_fv D`:

       `lstore_lift_free_restrict_lvars_subset_eq`.

     The remaining named proof obligation at this level is only the dependent
     `LWorldOn` path equation:

       `lworld_on_mlsubst_back_restrict_eq`.

     It should be proved by extensionality of the produced `LWorldOn`, not by
     rewriting through the `resA_product` compatibility proof term.

   Refined transport statement:

   - `denot_relevant_env Σ τ e` is only the denotation guard's syntactic
     restriction:

       `storeA_restrict Σ (context_ty_lvars τ ∪ tm_lvars e)`.

     It should not be the place where the fixed store typing premise is stated.
     The general transport should instead assume the ordinary well-scoped
     invariant:

       `denot_relevant_lvars τ e ⊆ dom Σ`

     and the fixed store is inside the original ambient environment:

       `lvars_of_atoms (dom σ) ⊆ dom Σ`.

   - Define the fixed-store typing premise on the original environment:

       `atom_store_has_ltype Σ σ :=
          forall x v, σ !! x = Some v ->
            exists T, Σ !! LVFree x = Some T /\ ∅ ⊢ᵥ v ⋮ T`.

     This pointwise form is stronger and clearer than `storeA_has_type Σ
     (lstore_lift_free σ)`: it requires every key actually present in `σ` to
     have a corresponding type in `Σ`, not merely keys that happen to overlap
     with `Σ`.

   - The environment part of substitution is just deletion of fixed atom keys:

       `lty_env_msubst_store σ Σ :=
          storeA_restrict Σ (dom Σ ∖ lvars_of_atoms (dom σ))`.

     Current implementation status:

     - `lstore_on_mlsubst_back` is in `ContextStore/StoreInterface.v`.
     - `qual_mlsubst` / `qual_msubst_store` are in
       `ContextTypeLanguage/Qualifier.v`.
     - `context_ty_msubst_store` and its lvar/erasure lemmas are in
       `ContextTypeLanguage/Syntax.v`.
     - `lty_env_msubst_store` and lookup/domain lemmas are in
       `ContextTypeLanguage/LtyEnv.v`.
     - `atom_store_has_ltype`, its domain-subset lemma, and the closed-store
       consequence are in `ContextBasicDenotation/StoreTyping.v`.
     - `ContextBasicDenotation/MsubstTransport.v` contains the models-level
       atom transports.  `context_ty_wf_formula_msubst_store_models` and
       `basic_world_formula_msubst_store_models` are proved, including the
       lower Store/Logic bridge `lworld_has_type_msubst_store_from_back`,
       which packages the direct product-store argument for
       `lworld_on_mlsubst_back`.  `expr_basic_typing_formula_msubst_store_models` and
       `expr_total_formula_msubst_store_models` remain the next atom-level
       obligations to prove from TermEval/BasicTypingFormula.

   - The main theorem should therefore be proved first in the relevant-store
     form:

       `denot_relevant_lvars τ e ⊆ dom Σ ->
        lvars_of_atoms (dom σ) ⊆ denot_relevant_lvars τ e ->
        atom_store_has_ltype Σ σ ->
        m ⊨ formula_msubst_store σ (ty_denote_gas gas Σ τ e)
        m ⊨ ty_denote_gas gas
              (lty_env_msubst_store σ Σ)
              (context_ty_msubst_store σ τ)
              (lstore_instantiate_tm (lstore_lift_free σ) e)`.

     This is intentionally one-way: for `type_qualifier_formula`, the frozen
     formula is stronger than the residual qualifier formula.  A full iff would
     be false without extra exactness assumptions on the qualifier predicate.

   - Public/full-fiber uses should first replace `σ` by

       `store_restrict σ (lvars_fv (denot_relevant_lvars τ e))`

     using `formula_msubst_store_restrict_fv_subset` together with
     `formula_fv_ty_denote_gas_subset_relevant`, then call the relevant
     theorem above.  This keeps extra selected-fiber variables from deleting
     irrelevant keys from the right-hand environment.

   - Add atom-formula transport lemmas, preferably models-level iff lemmas
     rather than exact equalities:

       `formula_msubst_store_expr_total_formula`
       `formula_msubst_store_expr_basic_typing_formula`
       `formula_msubst_store_expr_result_formula`
       `formula_msubst_store_type_qualifier_formula`
       `formula_msubst_store_context_ty_wf_formula`
       `formula_msubst_store_basic_world_formula`

     Each lemma should say that after substituting the selected base store into
     the formula, the semantics agrees with the corresponding instantiated
     syntax/context.  Exact equality is fine only when it is definitional and
     does not fight dependent qualifier paths.

     The two critical atom transports are:

       `expr_basic_typing_formula_msubst_store`:
          driven by `basic_tm_has_ltype_msubst_store`; variable cases split
          on whether the free variable is fixed by `σ`.

     `expr_total_formula_msubst_store`:
          driven by `expr_eval_in_store` and `lstore_instantiate_tm`; it should
          need only `store_closed σ`, which follows from
          `atom_store_has_ltype Σ σ`.

     Current proof decomposition for the two remaining atom transports:

     1. Term evaluation transport, placed at the TermEval/TermSyntax level:

          `expr_eval_in_store_msubst_back :
             store_closed σ ->
             expr_eval_in_store
               (lworld_on_mlsubst_back-style store τ plus σ restricted to
                tm_lvars e) e v ->
             expr_eval_in_store τ
               (lstore_instantiate_tm (lstore_lift_free σ) e) v`.

        The real content is a compositionality fact for
        `lstore_instantiate_tm`: instantiating first by the fixed free-store
        and then by the residual lstore is observationally the same as
        instantiating once by their compatible union.  This is the semantic
        reason that the `formula_msubst_store` atom can be transported.

     2. Basic typing transport, placed at BasicTypingFormula:

          `basic_tm_has_ltype_msubst_store :
             atom_store_has_ltype Σ σ ->
             basic_tm_has_ltype Σ e T ->
             basic_tm_has_ltype (lty_env_msubst_store σ Σ)
               (lstore_instantiate_tm (lstore_lift_free σ) e) T`.

        Its proof should split free-variable cases on membership in `dom σ`.
        Fixed variables are discharged using `atom_store_has_ltype`; residual
        variables are typed by the restricted environment.  Bound variables are
        still handled by the cofinite/open-env part of `basic_tm_has_ltype`.

     3. Formula-level atom transports in `MsubstTransport.v` should then only
        unpack `lqual_msubst_store`, apply the two semantic/syntax transports
        above, and repack the corresponding `res_models` implication.

        Important correction: for `type_qualifier_formula`, exact equality and
        a full iff are generally too strong.  `formula_msubst_store σ
        (type_qualifier_formula q)` says the back-world exactly characterizes
        the original predicate on all full `qual_vars q` stores.  By contrast,
        `type_qualifier_formula (qual_msubst_store σ q)` only characterizes the
        residual predicate after plugging in the fixed store.  The former
        implies the latter, but not conversely.  The proved lemma is therefore
        the sound direction:

          `type_qualifier_formula_msubst_store_models :
             m ⊨ formula_msubst_store σ (type_qualifier_formula q) ->
             m ⊨ type_qualifier_formula (qual_msubst_store σ q)`.

        Denotation transport should use this strong-to-weak direction in
        Over/Under/FibVars elimination, not an exact atom rewrite.





     Inductive BasicTyping refactor (chosen route):

     - Keep the old open-env specification under a compatibility name:

         `basic_tm_has_ltype_by_open_env Σ e T`

       This remains useful as a sanity/spec theorem and for proving that the
       new lvar-level typing agrees with the old CoreLang typing when the
       lty-env is lc/atom-only.

     - Redefine the public `basic_tm_has_ltype` as a real mutually-inductive
       lvar-level basic typing relation, mirroring CoreLang's `⊢ᵥ/⊢ₑ` rules
       but with `logic_var` keys and cofinite binders using `LVFree y`.

     - Prove regular lemmas for the new inductive relation:

         `basic_value_has_ltype_lvars`
         `basic_tm_has_ltype_lvars`
         `basic_value_has_ltype_lc` / `basic_tm_has_ltype_lc` if needed

     - Prove substitution/instantiation directly by mutual induction, but in
       a depth-general form because lambdas/fixes/lets recurse under binders:

         `basic_value_has_ltype_msubst_store_at d`
         `basic_tm_has_ltype_msubst_store_at d`
         `basic_tm_has_ltype_msubst_store` as the `d = 0` corollary

       Variable cases split on whether a free atom is fixed by `σ`; fixed
       variables use `atom_store_has_ltype`, residual variables use lookup in
       `lty_env_msubst_store`.  The closed-store invariant from
       `atom_store_has_ltype_closed` ensures substituted values add no lvars.


       Binder/variable support lemmas needed by this mutual induction are now
       explicit obligations in `BasicTypingFormula.v`:

         `basic_value_has_ltype_of_empty_atom_typing`
         `atom_store_has_ltype_insert_fresh`
         `lty_env_msubst_store_insert_fresh`

       These are intentionally small: the first handles fixed closed values,
       the second preserves fixed-store typing under fresh binders, and the
       third normalizes the environment in binder cases.

      Immediate proof order for this block:

      1. Prove the binder-environment normalization
         `lty_env_msubst_store_insert_fresh`.  This is a pure environment
         lemma and should not depend on term syntax.
      2. Prove lvar-support regularity for the new inductive typing relation:
         `basic_value_has_ltype_lvars` and `basic_tm_has_ltype_lvars`.
      3. Add/prove the atom-typing bridge for closed atom syntax.  The public
         helper `basic_value_has_ltype_of_empty_atom_typing` should be a small
         corollary of the more useful atom-env bridge, not a new proof path.
      4. Prove `basic_value_has_ltype_msubst_store_at` and
         `basic_tm_has_ltype_msubst_store_at` by the mutual induction above.
         Binder cases use steps 1 and 2 plus
         `atom_store_has_ltype_insert_fresh`.
         Status: done.  The proof now also has lvar weakening, atom-env
         typing bridge, and the fresh open/instantiate commute lemmas needed
         by binder cases.
         Follow-up status: the old empty-atom helper is now a corollary of a
         general atom-env bridge, and the closed lty-env roundtrip bridge is
         proved for the remaining compatibility theorem.
      5. Only after the inductive route is stable, repair compatibility lemmas
         (`*_to_open_env`, open/close/swap/tapp-tlete transport).  These are
         not allowed to drive the main msubst proof anymore.

     - Prove compatibility with the old open-env specification only as a
       bridge theorem, not as the definition.  This avoids the fragile
       arbitrary-`η` proof path in the main msubst proof.

     Refinement after simplifying `basic_tm_has_ltype`:

     - Since `basic_tm_has_ltype Σ e T` already contains
       `tm_lvars e ⊆ dom Σ`, its cofinite/open-env side conditions should be
       over `dom Σ`, not `dom Σ ∪ tm_lvars e`.  This keeps the definition
       focused on the context domain and makes the msubst transport statement
       shorter.
     - After this change, the remaining alpha/open-env issue for
       `basic_tm_has_ltype_msubst_store` is exactly about extending a residual
       opening environment for `dom Σ ∖ fixed` to one fresh for `dom Σ`, while
       preserving the opened residual term/context up to CoreLang typing.
     - The fixed atom store is completely closed in the proof.  This follows
       from `atom_store_has_ltype Σ σ`, since every fixed value is typed under
       the empty CoreLang environment.  We should use this invariant
       immediately:

         `atom_store_has_ltype Σ σ -> store_closed σ`

       The closedness invariant means fixed values contribute no fvars and no
       bvars.  Thus the remaining msubst obligations split cleanly into a
       pure alpha/open-env transport and an operational composition lemma for
       `lstore_instantiate_tm`; there is no extra variable support coming from
       the fixed values.

        Current implementation status:

        - `expr_basic_typing_formula_msubst_store_models` is proved from the
          named BasicTyping helper `basic_tm_has_ltype_msubst_store`.
        - `expr_total_formula_msubst_store_models` is proved from the named
          TermEval helper `expr_total_on_msubst_store_from_back` plus the
          lvar-support lemma
          `tm_lvars_lstore_instantiate_lift_free_closed`.
        - Lower-obligation status:
          (a) `basic_tm_has_ltype_msubst_store` is proved using the new
          inductive BasicTyping relation and no longer depends on the old
          arbitrary-open-env definition;
          (b) `expr_total_on_msubst_store_from_back` is proved.  The proof uses
          the back-world decomposition into a residual lstore and the fixed
          free store restricted to `tm_lvars e`;
          (c) `tm_lvars_lstore_instantiate_lift_free_closed` is proved from a
          depth-general mutual syntax lemma.

        - New reusable syntax support now available for the main denotation
          transport:

            `lstore_instantiate_lift_free_then_residual_at_mutual`
            `lstore_instantiate_tm_lift_free_then_residual`

          These state the operational composition law needed by total/result
          formulas: first instantiating by a closed lifted atom store and then
          by a residual lstore is the same as instantiating once by the union
          of the residual store and the fixed store restricted to the relevant
          lvars.

   - Then prove the main transport for type denotation:

       `ty_denote_gas_msubst_store_models`

     Shape, up to the final names:

       `m ⊨ formula_msubst_store σ (ty_denote_gas gas Σ τ e)
        <->
        m ⊨ ty_denote_gas gas Σ' τ' e'`

     where `Σ'`, `τ'`, and `e'` are the appropriate instantiated
     environment/type/term views.  If type/context instantiation is not yet
     fully defined, first prove the restricted versions needed by the active
     Soundness cases and keep the missing general syntax operation as a named
     obligation.

     Immediate sub-plan for this theorem:

     1. Add formula-level msubst transport lemmas for the two non-guard atoms
        that occur in recursive denotation bodies:

          `type_qualifier_formula_msubst_store_models`
          `expr_result_formula_msubst_store_models` (or the exact equality if
          its atom is genuinely extensional enough)

        These should be syntax/semantic alignment lemmas, not TLet-specific
        helpers.  They belong in BasicDenotation near the corresponding atom
        definitions.  For type qualifiers, keep the strong-to-weak
        models-level direction; exact equality is false in general because
        freezing a free store removes alternative values from the residual
        qualifier domain.

     2. Prove the guard transport as a reusable tactic/lemma that splits the
        outer nested `FAnd` and applies:

          `context_ty_wf_formula_msubst_store_models`
          `basic_world_formula_msubst_store_models`
          `expr_basic_typing_formula_msubst_store_models`
          `expr_total_formula_msubst_store_models`

     3. Prove `ty_denote_gas_msubst_store_models` by induction on gas.
        The structural cases use `res_models_and_iff`, `res_models_or`/
        `res_models_plus` maps, and the IH.  The Over/Under/Arrow/Wand cases
        should use the formula-level msubst normalization from step 1 plus
        existing impl/forall/wand mapping lemmas.

        Current status:

        - The gas-zero case is proved by `ty_denote_gas_zero_msubst_store_models_relevant`.
        - The structural Inter/Union/Sum branches are wired through the IH.
          Inter exposes the reusable obligation
          `ty_denote_gas_msubst_store_agree_relevant`, which records that
          the transformed denotation only observes the relevant part of the
          frozen atom store.
        - The Over/Under branches no longer contain anonymous admits in the
          induction proof.  They reduce to named formula-transport obligations:
          `ty_denote_gas_msubst_store_over_body` and
          `ty_denote_gas_msubst_store_under_body`.
        - The Arrow/Wand branches are also reduced to named body obligations,
          parameterized by the structural induction hypothesis:
          `ty_denote_gas_msubst_store_arrow_body` and
          `ty_denote_gas_msubst_store_wand_body`.
        - The main induction proof now has no anonymous admits and is closed
          with `Qed`, depending only on the named obligations above.
        - The first lower syntax lemma needed by the forall branches is now
          proved at Logic level: `formula_open_msubst_store_fresh`.
          `lqual_open_msubst_store_fresh` is also proved.  Its only remaining
          primitive dependency is the expected dependent-world path equation
          `lworld_on_open_mlsubst_back_fresh`, which says that open-back and
          mlsubst-back commute when the opened atom is fresh for the frozen
          atom store.

        Revised route after review:

        - Do not prove `ty_denote_gas_msubst_store_agree_relevant` as a
          special denotation lemma first.  Generalize the idea to arbitrary
          logic formulas:

            `formula_msubst_store_agree_fv`

          This says that `formula_msubst_store σ φ` only depends on `σ`
          restricted to `formula_fv φ`.  Then instantiate it with the existing
          bound
          `formula_fv_ty_denote_gas_subset_relevant_pre_open`, i.e. the
          denotation formula only sees `e` and `τ`.

        - Treat double msubst as Logic syntax infrastructure, not as a
          Denotation-specific workaround.  Reuse/prove the union normal form:

            `formula_msubst_store_union`

          plus fv-restrict variants when the two stores overlap only outside
          the relevant formula support.

        - Proof order from here:
          1. Keep/prove `formula_msubst_store_agree_fv`.
          2. Add the needed model-level corollary outside `FormulaSyntax`
             (where `res_models` is available).
          3. Prove or keep isolated the lower path helper
             `lworld_on_open_mlsubst_back_fresh`; the public qualifier and
             formula-level open/msubst lemmas should remain the interface used
             above it.
          4. Use these Logic-level tools to prove
             `ty_denote_gas_msubst_store_agree_relevant`.
             The statement must carry `store_closed σ1` and `store_closed σ2`.
             Without closed stores, a value stored at a relevant atom can
             introduce fresh lvars into the instantiated term; then the
             transformed denotation may observe lty-env entries outside the
             original `denot_relevant_lvars τ e`.
             Current status: proved.  The proof uses the lower support lemmas
             `context_ty_msubst_store_restrict_subset`,
             `lstore_instantiate_tm_lift_free_agree_subset`, and
             `lty_env_restrict_lvars_msubst_store_agree`, then finishes with
             the existing denotation env-agree transport.
          5. Use `formula_open_msubst_store_fresh` and the double-subst normal
             form to prove the Over/Under body transports.
             The result-atom part should not be specialized to opening depth
             `0`: after unfolding a `FForall`, the relevant open depth changes
             under nested binders.  First prove the term-level transport laws at
             arbitrary `k`:

             - `lstore_instantiate_tm_open_fresh_lift_free`
             - `lstore_instantiate_tm_shift_lift_free`

             with `store_closed σ` and `y ∉ dom σ`.  The Over/Under proof then
             normalizes `formula_open`/`formula_msubst_store`, applies
             `expr_result_formula_msubst_store_models`, and rewrites the
             instantiated `open`/`shift` term to the target result atom.
             Current status: the term-level helpers and the shared
             BasicDenotation wrapper
             `expr_result_formula_msubst_store_open_shift_models` are proved.
             The remaining Over/Under forall step also needs a small FV
             equality between the source msubsted body and target body:
             `formula_fv_over_msubst_store_body` /
             `formula_fv_under_msubst_store_body`.  These are currently named
             proved.  The proof also exposed and added the Base-level fact
             `lvars_fv_difference_singleton_bound`; a future cleanup can still
             move the repeated atom-fv normalization into the formula syntax
             normalizer for msubsted atoms
             (`basic_world_formula`, `expr_result_formula`,
             `type_qualifier_formula`) rather than by hand-expanding the whole
             denotation case.
          6. Use the same transport plus the induction hypothesis to prove
             Arrow/Wand body transports.

   Soundness layer:

   - Rewrite `sub`, `ctx-sub`, and `appop` through
     `res_models_FFibVars_impl_elim` plus the denotation msubst transport.
     Delete case-specific obligations such as
     `appop_intro_denotation_msubst` once the shared transport route is in
     place.

   - After the pilot cases compile, use the same route for TLet/Lam/App/Fix and
     the split cases.  The existing Denotation helpers should stay in their
     non-fiberwise shape; the new transport lemmas adapt the selected fiber to
     those helpers.

14. Logic-syntax mapper route for the remaining denotation msubst body admits.

   The remaining admits in `TypeDenoteMsubst.v` should not be proved
   by unfolding Over/Under/Arrow/Wand-specific denotation shapes.  The missing
   proof principle is a Logic-level mapper for formulas built from
   `FForall`, `FImpl`, `FFibVars`, `FOver`, and `FUnder`, combined with
   atom-level payload transports from BasicDenotation.

   New proof order:

   1. Keep the exact lower equality
      `lworld_on_open_mlsubst_back_fresh` proved in `LogicQualifier.v`.
      This is the only place that should reason about exact `LWorldOn`
      equality for open/mlsubst back maps.

   2. Keep the result atom payload transports in BasicDenotation:
      - `expr_result_formula_msubst_store_models`
      - `expr_result_formula_msubst_store_models_back`
      - `expr_result_formula_msubst_store_open_shift_models`
      - `expr_result_formula_msubst_store_open_shift_models_back`

      These are payload facts; Denotation should not unfold
      `expr_result_lqual` directly.

   3. Add Logic-level mapping lemmas in `FormulaConnectives.v`, not in
      Denotation:

      - `res_models_FFibVars_map`

        ```
        formula_scoped_in_world m (FFibVars D ψ) ->
        lc_lvars D ->
        (forall σ mfib,
           res_fiber_from_projection m (lvars_fv D) σ mfib ->
           mfib ⊨ formula_msubst_store σ φ ->
           mfib ⊨ formula_msubst_store σ ψ) ->
        m ⊨ FFibVars D φ ->
        m ⊨ FFibVars D ψ
        ```

      - Optional `res_models_over_map_same_world` and
        `res_models_under_map_same_world` only if the direct proof repeatedly
        unfolds `FOver`/`FUnder`.  Their shape should be simply:

        ```
        (m ⊨ φ -> m ⊨ ψ) -> m ⊨ FOver φ -> m ⊨ FOver ψ
        ```

        with the necessary scope premise supplied separately if Rocq needs it.

15. Current BasicTyping reverse-msubst status.

   Completed:

   - `basic_has_ltype_unique_mutual` is now proved.  The proof is by mutual
     induction on `basic_value_has_ltype` / `basic_tm_has_ltype`; the cofinite
     Lam/Fix/Let branches compare opened bodies at a common fresh atom.

   Remaining named obligation:

   - `basic_has_ltype_msubst_store_back_at_mutual`

     The naive syntax-induction proof is not strong enough.  In the Lam/Fix/Let
     cases, after inverting the transformed typing derivation we get a typing
     derivation for an opened transformed body:

     ```
     basic_tm_has_ltype
       (lty_env_msubst_store σ (<[LVFree y := T]> Σ))
       (lstore_instantiate_tm_at (S d) (lstore_lift_free σ)
          (open_tm 0 (vfvar y) e))
       U
     ```

     To reconstruct the source typing, we need the reverse-msubst theorem for
     `open_tm 0 (vfvar y) e`, not merely for `e`.  A plain syntax induction on
     `e` only gives the latter.  The correct proof route is therefore one of:

     - prove a cofinite strengthened reverse theorem whose induction hypothesis
       is available for opened bodies, or
     - prove the theorem by induction over the transformed typing derivation,
       carrying a relation between the source term and its instantiated target.

     The second route is likely cleaner for this inductive `basic_*_has_ltype`
     relation: each Lam/Fix/Let body premise is already a smaller typing
     derivation, so the recursive call lines up with the cofinite body proof.

      - Reuse existing `res_models_forall_map_same_fv` and
        `res_models_impl2_map`; do not introduce denotation-specific forall
        mappers.

   4. Prove Over/Under body transports in Denotation by the generic spine:

      ```
      res_models_forall_map_same_fv
      res_models_impl2_map
      res_models_FFibVars_map
      ```

      The first implication premise uses the existing basic-world atom
      transport.  The second premise uses the new back direction for
      `expr_result_formula`.  The final `FOver/FUnder(type_qualifier_formula)`
      payload uses `type_qualifier_formula_msubst_store_models`.

   5. Prove Arrow/Wand body transports with the same spine plus the induction
      hypothesis for the recursive denotation atoms.  If exact formula equality
      is too strong, use models-level transport and keep only the necessary
      FV equality/subset facts for `res_models_forall_map_same_fv`.

   6. After these four body transports compile, rerun full `make`, then commit.

   Progress update:

   - The Logic-level mapper lemmas now live in `FormulaConnectives.v`:
     `res_models_over_map`, `res_models_under_map`, and
     `res_models_FFibVars_map`.
   - The BasicDenotation payload layer now has sequential qualifier msubst
     transport:
     `type_qualifier_formula_msubst_store2_models` and
     `type_qualifier_formula_msubst_store_assoc_models`.
   - The Over/Under body transports in `TypeDenoteMsubst.v` are
     proved and compile.  Their last payload step uses the Logic-syntax mapper
     route plus the sequential qualifier msubst transport, not a
     denotation-shape-specific unfold.
   - A small TypeLanguage syntax lemma is now exposed as the remaining named
     obligation:

     ```
     qual_open_msubst_store_fresh :
       y ∉ dom σ ->
       qual_open_atom k y (qual_msubst_store σ q) =
       qual_msubst_store σ (qual_open_atom k y q)
     ```

     This is the type-qualifier analogue of the already-proved
     `lqual_open_msubst_store_fresh`.  The intended proof should use the
     generic `lstore_on_open_mlsubst_back_fresh`; the remaining nuisance is the
     dependent-domain cast produced by `qual_ext`.

   Remaining order:

   1. DONE: prove `qual_open_msubst_store_fresh` as an exact equality.  The
      proof follows `lqual_open_msubst_store_fresh`, using the Store-level
      `lstore_on_open_mlsubst_back_fresh` helper and an explicit `change` to
      avoid dependent-domain cast noise from `qual_ext`.
   2. Strengthen the recursive msubst induction interface from one-way models
      transport to models-level equivalence.  Arrow/Wand need the reverse
      direction in their negative argument position; the old one-way
      induction hypothesis is sufficient only for positive positions.
      Current code status:
      - `denot_msubst_induction_hyp` is now an `iff`.
      - The old one-way theorem is still present as
        `ty_denote_gas_msubst_store_models_relevant`.
      - The public iff wrapper is now
        `ty_denote_gas_msubst_store_models_relevant_iff`.
      - The reverse direction is isolated as the named obligation
        `ty_denote_gas_msubst_store_models_relevant_back`; this is the
        next proof target.
      - DONE: the BasicDenotation back direction now has formula-level
        bridges for:
        `context_ty_wf_formula_msubst_store_models_back`,
        `basic_world_formula_msubst_store_models_back`,
        `expr_basic_typing_formula_msubst_store_models_back`, and
        `expr_total_formula_msubst_store_models_back`.
      - DONE: the denotation guard reverse bridge is now exposed as
        `denot_guard_msubst_store_models_back_relevant`, and the gas-zero
        reverse bridge is
        `ty_denote_gas_zero_msubst_store_models_relevant_back`.
      - DONE: `lworld_has_type_lift_from_res_lift_free_same_dom` is proved.
        It is the resource/lift restriction fact needed by
        `basic_world_formula_msubst_store_models_back`.
      - Remaining named BasicDenotation sub-obligation introduced by this
        reverse route: `basic_tm_has_ltype_msubst_store_back`.  This is the
        real inductive reverse-substitution theorem for basic typing.
        Proof plan:
        1. Add lookup uniqueness support:
           `atom_store_has_ltype_lookup_from_basic_ltype`.  If `σ !! x = Some vx`
           and the substituted value `vx` has basic ltype `T` in the residual
           environment, then `Σ !! LVFree x = Some T`.  The proof uses
           `atom_store_has_ltype` to obtain some original type `U`, converts the
           residual basic-ltype fact for the closed value into CoreLang typing,
           and applies `basic_typing_unique_value`.
        2. Prove the arbitrary-depth mutual theorem
           `basic_has_ltype_msubst_store_back_at_mutual`.  The fvar-hit case
           uses the lookup uniqueness support; fvar-miss and bvar cases invert
           the residual lookup and use `lty_env_msubst_store_lookup_some`.
           Lam/Fix/Let use fresh `y ∉ L ∪ dom σ ∪ fv`, commute
           `lstore_instantiate_*_open0_fresh_lift_free`, then apply the IH
           under `atom_store_has_ltype_insert_fresh` and
           `lty_env_msubst_store_insert_fresh`.
        3. Derive `basic_tm_has_ltype_msubst_store_back` as the `d = 0`
           wrapper.
        Updated route chosen for the current proof:
        - Do not prove the reverse theorem by source syntax induction.  The
          body cases need the induction hypothesis for opened bodies, and the
          source syntax IH is not strong enough.
        - Prove
          `basic_has_ltype_msubst_store_back_by_derivation` by induction over
          the transformed `basic_value_has_ltype` / `basic_tm_has_ltype`
          derivation.  Carry equalities saying the transformed value/term is
          exactly `lstore_instantiate_*_at d (lstore_lift_free σ)` of some
          source value/term.
        - In Lam/Fix/Let, choose fresh `y ∉ L ∪ dom σ ∪ fv(body)`, insert
          `y` into both the source typing environment and the typed atom store,
          commute open with closed-store instantiation, and apply the body IH
          to the smaller transformed body derivation.
        - Keep `basic_has_ltype_msubst_store_back_at_mutual` only as a thin
          wrapper around this derivation-induction theorem, and derive
          `basic_tm_has_ltype_msubst_store_back` from the `d = 0` instance.
        - Small normalization helpers used by this route:
          `atom_store_has_ltype_lookup_from_basic_ltype`,
          `basic_value_has_ltype_msubst_store_back_fvar`, and the closed-store
          bvar fact
          `lstore_instantiate_value_at_lift_free_bvar`.
        Current status:
        - DONE: `basic_has_ltype_msubst_store_back_by_derivation` is proved
          and `ContextBasicDenotation/theories/BasicTypingFormula.vo`
          compiles.
        - DONE: `basic_has_ltype_msubst_store_back_at_mutual` and
          `basic_tm_has_ltype_msubst_store_back` are now thin wrappers over the
          derivation-induction theorem.
        - DONE: `ContextBasicDenotation/theories/MsubstTransport.vo` and
          `Denotation/theories/TypeDenoteMsubst.vo` compile with
          the remaining Denotation-level named obligations isolated.
        - Remaining proof work in this msubst block is now genuinely at the
          Denotation Arrow/Wand level: the body transports must combine
          `formula_open_msubst_store_fresh`, the denotation open transport, and
          the iff-shaped induction hypothesis in negative/positive positions.
          This likely needs small syntax-normalization helpers for the Arrow
          and Wand forall bodies, analogous to
          `formula_fv_over_msubst_store_body` and
          `formula_fv_under_msubst_store_body`, plus the already available
          `res_models_impl2_map` / `res_models_impl_wand_map` spine.
   3. Prove Arrow body transport by the same `FForall`/`FImpl` mapper spine.
      The first implication premise uses the reverse projection of the
      strengthened induction hypothesis; the result premise uses the forward
      projection after normalizing:
      - `context_ty_msubst_store` through `cty_shift 0`;
      - `lty_env_msubst_store` through `typed_lty_env_bind`;
      - `lstore_instantiate_tm` through `tm_shift 0` and `tapp_tm`.
      Concrete proof spine:
      - define `src` and `dst` forall bodies, prove
        `formula_fv (formula_msubst_store σ src) = formula_fv dst`;
      - apply `res_models_forall_map_same_fv`;
      - after opening the fresh forall atom, normalize
        `formula_open`/`formula_msubst_store`;
      - apply `res_models_impl2_map`;
      - world premise is transported by the same freshness argument used by
        Over/Under;
      - argument premise is contravariant, so use the backward half of
        `denot_msubst_induction_hyp gas`;
      - result premise is covariant, so use the forward half of the same IH.
      Progress:
      - DONE: added and compiled
        `context_ty_msubst_store_open_fresh`; this removes one recurring
        fresh-open normalization gap after the forall witness is chosen.
      - DONE: added and compiled
        `formula_open_ty_denote_gas_singleton`; this packages the common
        singleton-open exact rewrite for recursive denotation atoms, so the
        Arrow/Wand proof can normalize each premise before applying the IH.
      - DONE: added and compiled Store-level
        `lvar_store_shift_from_atom_store`; this is the bottom fact needed to
        prove that atom-store msubst commutes with context-type shifting.
      - DONE: added and compiled Base-level
        `lvars_shift_from_difference_of_atoms`; this normalizes the domain
        side of `qual_shift_from` commuting with atom-store msubst.
      Next proof steps:
      1. Store layer: prove the atom-store specialization of shift commuting
         with `lstore_on_mlsubst_back`.  The intended statement should avoid
         dependent casts by proving equality of the underlying stores:
         `lso_store (lstore_on_shift_from k (lstore_on_mlsubst_back D ρ s)) =
          lso_store (lstore_on_mlsubst_back (lvars_shift_from k D) ρ s')`,
         where `ρ = atom_store_to_lvar_store σ` and `s'` is the shifted input
         store coerced through
         `lvars_shift_from_difference_of_atoms`.
         DONE: `lstore_on_shift_from_mlsubst_back_atom_store` is proved in
         `ContextStore/StoreInterface.v`.
         DONE: the inverse/reconstruction directions needed by
         `qual_shift_from_msubst_store` are proved by first proving raw
         `gmap` statements and then wrapping them back through `LStoreOn`:
           `lstore_shift_from_restrict_of_mlsubst_back_atom_store_raw`
           `lstore_mlsubst_back_restrict_of_shift_atom_store_raw`
           `lstore_on_shift_from_restrict_of_mlsubst_back_atom_store`
           `lstore_on_mlsubst_back_restrict_of_shift_atom_store`
         The key lesson is that the proof should avoid dependent record paths
         until the final wrapper.  The raw lemmas use lookup extensionality over
         `logic_var`, with `lookup_kmap`, `storeA_restrict_lookup`,
         `atom_store_to_lvar_store_lookup_bound_none`, and
         `atom_store_to_lvar_store_dom`.
      2. Qualifier layer: prove exact
         `qual_shift_from k (qual_msubst_store σ q) =
          qual_msubst_store σ (qual_shift_from k q)` using `qual_ext`.
         DONE: proved in `ContextTypeLanguage/theories/Qualifier.v`; it now
         depends only on the two named Store reconstruction obligations above.
      3. Type layer: prove exact
         `cty_shift k (context_ty_msubst_store σ τ) =
          context_ty_msubst_store σ (cty_shift k τ)` by induction on `τ`.
         DONE: proved as `context_ty_msubst_store_shift` in
         `ContextTypeLanguage/theories/Syntax.v`.
      4. Denotation layer: use this shift/msubst rewrite in the Arrow/Wand
         body transports before applying the iff-shaped recursive IH.
         CURRENT: `Denotation/theories/TypeDenoteMsubst.vo`
         compiles after these lower transports.  The remaining named
         Denotation obligations are:
           `ty_denote_gas_msubst_store_arrow_body`
           `ty_denote_gas_msubst_store_wand_body`
           `ty_denote_gas_msubst_store_models_relevant_back`
         Updated concrete move:
         - Strengthen Arrow/Wand body transport to iff-shaped helpers (or prove
           explicit `_back` siblings first and keep the existing forward names
           as projections).  This is necessary because
           `ty_denote_gas_msubst_store_models_relevant_back` is an
           induction that must cross Arrow/Wand in the reverse direction.
         - Prove Arrow with the same `res_models_forall_map_same_fv` +
           `res_models_impl2_map` spine used by Over/Under.  Normalize the
           opened source with `formula_open_msubst_store_fresh`,
           `formula_open_ty_denote_gas_singleton`,
           `context_ty_msubst_store_shift`, and the term-open/shift lemmas for
           `tapp_tm`.  In the forward direction, use the backward half of
           `denot_msubst_induction_hyp` on the argument premise and the forward
           half on the result premise.  In the backward direction, swap those
           uses.
         - Prove Wand analogously with `res_models_impl_wand_map`; its
           antecedent is contravariant and its consequent is covariant under
           `res_product`.
         - Then prove `ty_denote_gas_msubst_store_models_relevant_back` by
           induction on gas/types, using the new Arrow/Wand back projections.
         Immediate execution checklist:
         1. Add/prove Arrow/Wand forall-body FV normalization lemmas, analogous
            to `formula_fv_over_msubst_store_body` and
            `formula_fv_under_msubst_store_body`.  These should be syntactic
            lemmas only; they justify `res_models_forall_map_same_fv`.
            Support added while preparing this step:
            - DONE: `lty_env_msubst_store_open_one_fresh`, commuting a fresh
              singleton open with atom-store msubst on type environments.
            - DONE: `lty_env_msubst_store_shift`, commuting type-environment
              shift with atom-store msubst.
            - DONE: `lty_env_msubst_store_insert_bound0_shift` and
              `lty_env_msubst_store_typed_bind`, so the Arrow/Wand bound
              parameter environment can be normalized without expanding
              `typed_lty_env_bind`.
         2. Prove forward Arrow/Wand body transports by mapping the forall body.
            After `formula_open_msubst_store_fresh` and
            `formula_open_ty_denote_gas_singleton`, use the IH backward on
            the argument premise and IH forward on the result premise.
         3. Add/prove reverse Arrow/Wand body transports with the same proof
            skeleton but opposite IH directions.
         4. Re-run the gas/type induction for
            `ty_denote_gas_msubst_store_models_relevant_back`; all
            non-Arrow/Wand cases should mirror the existing forward proof with
            the iff-shaped IH.
            Progress:
            - DONE: the public back theorem now first restricts the input store
              to `denot_relevant_lvars τ e` and then calls the restricted
              theorem.  This proves the non-inductive "arbitrary σ" wrapper and
              leaves only the relevant-store induction:
              `ty_denote_gas_msubst_store_models_relevant_back_restricted`.
              This matches the relevant-store discipline used by the forward
              theorem and avoids carrying irrelevant atom substitutions through
              every recursive case.
            - Next BasicDenotation helper:
              `type_qualifier_formula_msubst_store_assoc_models_back`.
              Proof shape: unfold the two nested `formula_msubst_store`s and
              choose the witness
              `lstore_on_mlsubst_back (D ∖ dom ρ1) ρ2 s` for the intermediate
              qualifier.  Then `lstore_in_lworld_on_mlsubst_back` converts the
              world-membership side.  No union-composition lemma is needed for
              this back direction because we keep the two back substitutions
              nested instead of merging them.
   4. Prove Wand body transport analogously, replacing the inner implication
      mapper with the existing wand connective mapper or adding the Logic-level
      `res_models_wand_map` if it is not already present.
      Concrete proof spine:
      - same forall and first-impl normalization as Arrow;
      - apply `res_models_impl_wand_map`;
      - wand antecedent is contravariant and uses the backward IH;
      - wand consequent is covariant under the product world and uses the
        forward IH.
   5. Rerun `make Denotation/theories/TypeDenoteMsubst.vo`, then
      full `make`, then commit.

14. Revised singleton-projection route for `ty_denote_gas_msubst_store`.

   The previous route tried to prove a global transport between

     `formula_msubst_store σ (ty_denote_gas gas Σ τ e)`

   and

     `ty_denote_gas gas
        (lty_env_msubst_store σ Σ)
        (context_ty_msubst_store σ τ)
        (lstore_instantiate_tm (lstore_lift_free σ) e)`.

   This is not the right semantic theorem for the actual use site.  It forces
   Arrow/Wand negative positions to use a reverse transport for type
   qualifiers, which would require

     `type_qualifier_formula (qual_msubst_store σ φ) ->
      formula_msubst_store σ (type_qualifier_formula φ)`.

   That implication is false for ordinary dependent exact-world qualifiers.
   Example shape: `φ` says `ν > x`, `σ` fixes `x = 5`; the normalized
   qualifier only checks `ν > 5`, while the raw substituted exact-world atom
   also rejects full stores that satisfy `ν > x` with a different `x`.

   The actual Soundness/Context use site never supplies an arbitrary `σ`.
   It obtains `σ` from the outer `FFibVars (ctx_base_vars Σ)` projection:

     `res_fiber_from_projection m (dom Σ) σ mfib`.

   Hence the chosen fiber world `mfib` has a singleton projection on `dom σ`,
   exposed by

     `res_restrict_fiber_from_projection_dom_singleton`.

   The new live theorem should therefore be singleton-aware and should not
   rewrite the context type itself:

     `m ⊨ formula_msubst_store σ (ty_denote_gas gas Σ τ e) <->
      m ⊨ ty_denote_gas gas
            (lty_env_msubst_store σ Σ)
            τ
            (lstore_instantiate_tm (lstore_lift_free σ) e)`.

   It carries a premise saying that `m` has singleton projection `σ` on the
   relevant fixed variables.  A convenient first statement is:

     `res_restrict m (dom σ) = singleton_world σ`.

   Later wrappers can derive this premise from `res_fiber_from_projection`.

   Immediate proof checklist:

   1. DONE: Logic layer: prove the exact-domain commuting lemma for fiber
      formulas:

        `res_models_msubst_store_fibvars_singleton_exact_iff`

      Statement:

        `lc_lvars K ->
         dom σK = lvars_fv K ->
         (res_restrict m (lvars_fv K) : WorldT) = singleton_world σK ->
         formula_scoped_in_world m (FFibVars K p) ->
         m ⊨ formula_msubst_store σK (FFibVars K p) <->
         m ⊨ FFibVars K p`.

      Proof plan:
      - Normalize `formula_msubst_store σK (FFibVars K p)` to
        `FFibVars (K ∖ lvars_of_atoms (dom σK))
          (formula_msubst_store σK p)`.
      - Use `lc_lvars K` and `dom σK = lvars_fv K` to prove the residual
        lvar set is empty.
      - Reduce the left side with `res_models_FFibVars_empty_iff`.
      - Reduce the right side with the existing singleton eliminator/intro:
        `res_models_FFibVars_singleton_elim` and
        `res_models_FFibVars_singleton_intro`.

   2. DONE: Logic layer: prove a wrapper for bigger stores:

        `res_models_msubst_store_fibvars_singleton_iff`

      Statement:

        `lc_lvars K ->
         (res_restrict m (lvars_fv K) : WorldT) =
           singleton_world (store_restrict σ (lvars_fv K)) ->
         dom σ ∩ formula_fv p ⊆ lvars_fv K ->
         formula_scoped_in_world m (FFibVars K p) ->
         m ⊨ formula_msubst_store σ (FFibVars K p) <->
         m ⊨ FFibVars K p`.

      Proof plan:
      - Use `formula_msubst_store_restrict_subset`/agree-on-fv to replace
        `σ` by `store_restrict σ (lvars_fv K)` for the whole `FFibVars K p`.
      - Derive the exact-domain side condition for the restricted store.
      - Call the exact-domain lemma.

   3. DONE: Algebra/Logic wrapper: expose the projection form used by Context:

        `res_models_msubst_store_fibvars_from_projection_iff`

      Statement shape:

        `res_fiber_from_projection m X σ mfib ->
         lvars_fv K ⊆ X ->
         dom σ ∩ formula_fv p ⊆ lvars_fv K ->
         formula_scoped_in_world mfib (FFibVars K p) ->
         mfib ⊨ formula_msubst_store σ (FFibVars K p) <->
         mfib ⊨ FFibVars K p`.

      Proof plan:
      - From `res_fiber_from_projection` get
        `res_restrict mfib (dom σ) = singleton_world σ`.
      - Restrict that singleton equality down to `lvars_fv K` using
        `lvars_fv K ⊆ dom σ` or a derived projection-domain equality.
      - Apply the wrapper from step 2.

   4. Denotation layer: replace the old msubst transport targets.

      Stop proving/using:

        `ty_denote_gas_msubst_store_models_relevant_back_restricted`
        with target `(context_ty_msubst_store σ τ)`.

      Add the singleton-aware theorem:

        `ty_denote_gas_msubst_store_singleton_iff`.

      Updated statement shape after the wf discussion:

        `basic_context_ty_lvars (dom Σ) τ ->
         tm_lvars e ⊆ dom Σ ->
         atom_store_has_ltype Σ σ ->
         (res_restrict m (lvars_fv (denot_relevant_lvars τ e)) : WorldT) =
           singleton_world (store_restrict σ
             (lvars_fv (denot_relevant_lvars τ e))) ->
         m ⊨ formula_msubst_store σ (ty_denote_gas gas Σ τ e) <->
         m ⊨ ty_denote_gas gas
              Σ
              τ
              (lstore_instantiate_tm (lstore_lift_free σ) e)`.

      Important correction: the target must keep both `Σ` and `τ` unchanged.
      Only the term is instantiated.  The earlier target
      `(lty_env_msubst_store σ Σ, τ)` is too weak for
      `context_ty_wf_formula`, because deleting `dom σ` from `Σ` while
      leaving `τ` unchanged can make the guard false.  The even earlier target
      `(lty_env_msubst_store σ Σ, context_ty_msubst_store σ τ)` is also wrong
      for Arrow/Wand negative positions.

      The wf premises are not cosmetic.  The proof needs them to know that all
      type variables read by `τ` are covered by the singleton projection, and
      that recursive Arrow/Wand subtypes remain well-scoped when the induction
      hypothesis is applied.  `basic_context_ty_lvars (dom Σ) τ` is preferred
      over ad-hoc FV/lc premises because it packages both lc/no-bvar shape and
      free-variable inclusion.

      If the proof is cleaner, first state it with
      `(res_restrict m (dom σ) : WorldT) = singleton_world σ` and
      `lvars_of_atoms (dom σ) ⊆ denot_relevant_lvars τ e`; then add an
      arbitrary-store wrapper using `store_restrict`.

      Immediate decomposition before touching the recursive induction:

      - Add a singleton-aware guard theorem for gas-zero:

          `denot_guard_msubst_store_singleton_iff`.

        It should keep `Σ` and `τ` unchanged and only instantiate the term.
        The proof should not try to prove a global qualifier back transport.
        Instead:
        * `context_ty_wf_formula` comes from the explicit
          `basic_context_ty_lvars (dom Σ) τ` premise plus the scopedness
          supplied by the source/target guard;
        * `basic_world_formula` uses the source residual world plus the
          singleton typed store to reconstruct the fixed atoms, then weakens
          to the target `denot_relevant_env`;
        * `expr_basic_typing_formula` uses the existing residual msubst typing
          theorem plus weakening/reconstruction from the singleton typed store;
        * `expr_total_formula` uses the already proved total msubst transport.

      - Then prove:

          `ty_denote_gas_zero_msubst_store_singleton_iff`.

      - Only after gas-zero is clean, define the singleton-shaped recursive IH.

      Atom-level proof decomposition for
      `denot_guard_msubst_store_singleton_iff`:

      - DONE `context_ty_wf_formula`: prove directly from
        `basic_context_ty_lvars (dom Σ) τ`; the atom is world-independent
        except for lc/scope of the domain, and the target relevant env domain is
        a restriction of `Σ`.  The proof is now explicit about
        `lvars_fv`/`lvars_of_atoms` membership instead of relying on slow
        `set_solver` calls.
      - DONE `basic_world_formula`: add a BasicDenotation reconstruction lemma.
        From `m ⊨ formula_msubst_store σ (basic_world_formula Σg)`,
        `atom_store_has_ltype Σ σ`, and the singleton projection of `m` on
        relevant variables, reconstruct
        `m ⊨ basic_world_formula Σg'`.  The proof splits lookups into residual
        variables handled by the msubst atom and fixed variables handled by
        `atom_store_has_ltype` plus the singleton equality.  The reusable
        lower-level lemma is
        `basic_world_formula_fixed_from_singleton`.
      - IN PROGRESS `expr_basic_typing_formula`: the standalone iff is false
        without local-closure/wf information.  Counterexample shape:
        a lambda body can mention a dangling `vbvar 1`; the lambda's
        `tm_lvars` records this as `LVBound 0`, while the opened body typing
        can consume `LVBound 1` if the target relevant environment picked it up
        from `τ`.  Therefore the singleton atom lemma now carries `lc_tm e`;
        if the final denotation proof needs the backward direction for types
        with bound vars, also add `lc_context_ty τ` or replace
        `basic_context_ty_lvars` with the stronger wf predicate at this
        boundary.

        New lower helpers already added/proved:

        * `value_lvars_lc_eq_atoms`
        * `tm_lvars_open_body_subset_lc`
        * `value_lvars_open_body_subset_lc`
        * `lstore_instantiate_tm_lift_free_lc`
        * `basic_*_has_ltype_restrict_lvars_lc`

        Remaining proof steps for the atom:

        * Forward direction: use
          `expr_basic_typing_formula_msubst_store_models` on the restricted
          store, rewrite the instantiated term from
          `lstore_lift_free (store_restrict σ (lvars_fv R))` to
          `lstore_lift_free σ` with
          `lstore_instantiate_tm_lift_free_agree_subset`, then weaken the
          residual environment into the same-`Σ` target relevant environment.
        * Backward direction: destruct target formula models, restrict the
          lc target typing derivation to `tm_lvars e'` using
          `basic_tm_has_ltype_restrict_lvars_lc`, weaken that restricted env
          into the residual msubst env, then call
          `expr_basic_typing_formula_msubst_store_models_back`.
      - `expr_total_formula`: reuse the existing total msubst transport; it is
        independent of the typing environment.

   5. Over/Under denotation cases.

      In the opened forall body, instantiate the Logic lemma with:

        `K := qual_vars (φ ^q^ y) ∖ {[LVFree y]}`
        `p := FOver/FUnder (type_qualifier_formula (φ ^q^ y))`.

      Freshness `y ∉ dom σ` ensures the result variable is not fixed by the
      singleton store.  The set side condition should reduce to:

        `dom σ ∩ qual_dom (φ ^q^ y) ⊆ lvars_fv K`.

      This is a set/fv normalization obligation, not a semantic qualifier
      transport.

   6. Arrow/Wand denotation cases.

      Because the target type remains `τ` rather than
      `context_ty_msubst_store σ τ`, the recursive IH is now an iff over the
      same source context type.  The argument premise can use the backward half
      without ever needing a false type-qualifier back lemma; the result premise
      uses the forward half.

      Wand is not a counterexample.  Although the wand antecedent is checked in
      an arbitrary compatible frame `n`, the parameter type may only refer to
      the base `Σ` variables, not captured `Γ` variables.  If
      `res_product n m Hc ⊑ M` and `M` has singleton projection on the relevant
      `Σ` variables, then `n` inherits the same singleton projection on
      `FV(τx)`.  Use the existing singleton-product inheritance lemmas, e.g.
      `res_product_le_singleton_restrict_inv` or a thin wrapper specialized to
      the world appearing in the wand proof.

   7. Context/Soundness call sites.

      The current elimination lemma

        `ty_denote_under_fiber_elim_projection`

      already exposes

        `res_fiber_from_projection m (dom Σ) σΣ mfib`
        and
        `mfib ⊨ formula_msubst_store σΣ (...)`.

      Use the new projection wrapper to obtain the singleton-aware denotation
      transport at exactly this boundary.

   8. Compile order:

      - `make ContextLogic/theories/FormulaConnectives.vo`
      - `make Denotation/theories/TypeDenoteMsubst.vo`
      - `make Denotation/theories/Context.vo`
      - `make ContextTyping/theories/Soundness.vo`

   Any old named obligation that only existed to prove the false global back
   transport should be deleted or marked obsolete in comments before final
   cleanup.

   9. Refinement after implementing the first guard helpers.

      The Soundness-facing boundary still uses `base_store_projection`, but the
      recursive induction hypothesis should not require a full base projection
      on every intermediate world.  In `FPlus`, `FStar`, and `FWand` branches
      the recursive call may run on a subresource/product component whose world
      need not contain unused base variables.  Requiring

        `base_store_projection Σbase σ m'`

      for those intermediate worlds is therefore too strong.  The stable
      internal premise should be the local projection on the actually
      substituted store:

        `(res_restrict m' (dom σ) : WorldT) = singleton_world σ`.

      The public theorem can derive this local premise from
      `base_store_projection Σbase σ m`, while recursive branches can derive it
      by restricting `σ` to the formula/relevant variables they actually use
      and applying the existing singleton inheritance lemmas:

      - `res_subset_singleton_restrict`
      - `res_product_le_singleton_restrict_inv`
      - `res_product_le_singleton_pullback`
      - `res_sum_le_singleton_restrict_inv`

      The already-proved guard helpers are compatible with this direction:
      `context_ty_wf_formula_msubst_store_fixed_scope_iff` only needs fixed
      variables in scope, and
      `basic_world_formula_msubst_store_fixed_singleton_iff` only needs the
      fixed overlap to be singleton.  The next proof step should therefore add
      a small local-projection predicate/lemma family and use it as the
      internal IH, keeping `base_store_projection` as only the external
      Soundness bridge.
