(** * ContextTyping.SoundnessFixD

    Excluded historical draft.

    This file is intentionally not listed in [_CoqProject].  The old FixD
    typing rule was removed from the main development, so the previous proof
    sketch is no longer part of the compiled surface.  If FixD is redesigned,
    recover the old draft from git history and rebuild it against the current
    global primitive-operation context and normalization discipline. *)
