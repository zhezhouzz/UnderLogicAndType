# >1s/KLOC Optimization Log

This log tracks optimization batches driven by `CompileTimeStats.txt`.

## Candidate Policy

- Treat every file above `1 s/KLOC` as having possible optimization room.
- Prioritize files that are both high total time and high density.
- Ignore tiny re-export files as primary targets unless they hide avoidable
  imports; their density is dominated by fixed Rocq startup/import overhead.
- Each batch should identify a concrete slow proof pattern, replace it with a
  smaller lemma/tactic/normalization step, compile the affected file, and then
  commit.

## Batch 1: `TypeEquivTermTLet.v`

- Baseline from `CompileTimeStats.txt`: `7.64s`, `20.70 s/KLOC`.
- Located slow proof fragment with `COQEXTRAFLAGS=-time` partial log:
  the largest completed hotspot was a `set_solver` inside
  `tm_fiber_equiv_tlete_body_extension`, costing about `2.11s`.
- Optimization:
  - Added local syntax/FV helpers:
    `fv_tm_open_fvar_tlete_body_subset`,
    `fv_tm_tlete_left_subset`,
    `notin_fv_tm_tlete`.
  - Replaced the expensive generic set proof around
    `store_restrict_insert_agree_on_observed` with these direct helpers.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypeEquivTermTLet.vo`
    passed.
  - New focused time observed: `5.55s`.

## Batch 2: `TypeEquivTermTLet.v` store/domain cleanup

- Starting point after Batch 1: `5.55s`.
- Located remaining slow commands with `COQEXTRAFLAGS=-time`; the largest
  remaining hotspots were `set_solver` calls in the second half of
  `tm_fiber_equiv_tlete_body_extension`, especially the store-agreement block
  that proves `z` is fresh for an updated store.
- Optimization:
  - Added local helpers `elem_of_fv_tm_open_fvar` and
    `notin_store_union_singleton_dom`.
  - Replaced repeated `set_solver` calls for `a <> x`, `a ∈ fv_tm (tlete ...)`,
    singleton output-domain exclusion, and `z ∉ dom (σm ∪ {[x := vx]})` with
    direct FV/domain reasoning.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypeEquivTermTLet.vo`
    passed.
  - New focused time observed: `3.58s`.

## Batch 3: `TypePersistWandReverse.v`

- Baseline from `CompileTimeStats.txt`: `46.78s`, `38.09 s/KLOC`.
- A `COQEXTRAFLAGS=-time` run timed out at 90s, but the partial log showed
  the completed dominant hotspot:
  - `replace (world_dom m ∪ ({[y]} ∪ ∅)) with ... by set_solver`,
    costing about `21.94s`.
  - Four repeated `cbn [fv_tm fv_value]; set_solver` side conditions for
    empty term FV, each around `1.3s` in the partial log.
- Optimization:
  - Added local lemma `union_singleton_empty_r`.
  - Replaced the expensive domain-association `set_solver` calls with the
    local lemma.
  - Replaced empty-FV set searches with `not_elem_of_empty`.
- Verification:
  - `timeout 80s make TIMED=1 Denotation/theories/TypePersistWandReverse.vo`
    passed.
  - New focused time observed: `0.99s`.

## Batch 4: `TypePersistWandForward.v`

- Baseline from `CompileTimeStats.txt`: `13.23s`, `14.21 s/KLOC`.
- `COQEXTRAFLAGS=-time` completed and showed two dominant hotspots:
  - two `cbn [fv_tm fv_value]; set_solver` side conditions, about `6.0s`
    each, inside `formula_open_ty_denote_gas_singleton` calls.
  - one smaller singleton-union set proof near the persistent-over argument
    fiber construction.
- Optimization:
  - Added local lemma `empty_union_singleton_l`.
  - Replaced the singleton-union proof with that lemma.
  - Replaced both empty-FV set searches with `not_elem_of_empty`.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypePersistWandForward.vo`
    passed.
  - New focused time observed: `1.04s`.

## Batch 5: `SoundnessLamWand.v`

- Baseline from `CompileTimeStats.txt`: `10.85s`, `13.01 s/KLOC`.
- `COQEXTRAFLAGS=-time` showed:
  - a `set_solver` in the opened product-domain calculation costing `4.44s`;
  - three repeated `ctx_erasure_under_norm_in Hy_rest; better_set_solver`
    freshness proofs costing about `1.8-2.0s` each.
- Optimization:
  - Added local `union_singleton_empty_r` for the opened one-variable domain
    equation.
  - Derived erased-context freshness once as `Hy_eraseΓ`, then derived `HyΔ`
    once and reused it in the later `typed_lty_env_bind_open_current` and
    weakening side conditions.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessLamWand.vo`
    passed.
  - New focused time observed: `5.86s`.

## Batch 6: `SoundnessFixApply.v`

- Baseline from `CompileTimeStats.txt`: `12.19s`, `14.34 s/KLOC`.
- `COQEXTRAFLAGS=-time` initially showed:
  - a reflexive world-domain inclusion proof in
    `expr_result_formula_at_of_result_extends_on_coarsen` costing about
    `4.84s`;
  - repeated freshness/domain side proofs around the nested result-extension
    drop, including a duplicated `ctx_erasure_under` domain proof.
- Optimization:
  - Replaced reflexive inclusion side conditions with direct `intros a Ha;
    exact Ha`.
  - Hoisted the context-erasure domain inclusion and `z`-fresh-for-`Δy`
    proof out of the inner static-guard block so both later drops can reuse
    the same `HzΔ`.
  - Reused the earlier `Hwz` / `HwΔ` / `HwΣrel` facts in the `w` drop instead
    of re-solving the same set-map side conditions.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessFixApply.vo`
    passed.
  - Focused `-time` run passed.
  - New focused time observed: `5.85s`.
  - Remaining largest timed sentences are now about `0.98s`
    (`ty_denote_gas_tapp_result_alias`) and `0.83s`
    (`ty_denote_under_comma_bind_inv`), so no single multi-second solver
    remains in this file.

## Batch 7: `SoundnessFixBase.v`

- Baseline from `CompileTimeStats.txt`: `6.39s`, `14.01 s/KLOC`.
- `COQEXTRAFLAGS=-time` showed one dominant hotspot:
  - a `set_solver` in `ty_denote_lt_arg_fiber` costing about `5.03s`.
  - The goal was only the fact that `LVFree x` belongs to
    `relevant_lvars τlt (tret (vfvar x))`, via the term-lvars component.
- Optimization:
  - Replaced the broad `set_solver` after unfolding `relevant_lvars` with the
    direct proof through the right union branch and singleton membership.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessFixBase.vo`
    passed.
  - New focused time observed: `0.94s`.

## Batch 8: `ConstDenoteBase.v`

- Baseline from `CompileTimeStats.txt`: `6.41s`, `7.75 s/KLOC`.
- `COQEXTRAFLAGS=-time` showed one dominant hotspot:
  - a `set_solver` inclusion side condition in
    `ty_denote_over_ret_fvar_base_const`, inside
    `expr_result_formula_at_coarsen_domain`, costing about `4.37s`.
  - The goal was just
    `dom (relevant_env ...) ⊆ dom (relevant_env ...) ∪ tm_lvars ...`.
- Optimization:
  - Replaced the broad set search with the direct left-union inclusion proof:
    `intros v Hv; apply elem_of_union_l; exact Hv`.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/ConstDenoteBase.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `1.27s`.

## Batch 9: `FormulaFiberwise.v`

- Focused `COQEXTRAFLAGS=-time` run before this batch showed one dominant
  hotspot:
  - a `set_solver` domain equality proof in
    `fiberwise_joinable_on_fbwand1`, costing about `3.68s`;
  - the goal was just the opened `FBWand` product-domain condition after
    rewriting both argument and closure fibers back to their source domains.
- Optimization:
  - Replaced the broad set search with a direct membership proof using the
    already available product-domain hypothesis `Hdom`.
  - The proof now handles the harmless `{[y]} ∪ ∅` residue explicitly instead
    of asking the set solver to normalize the whole context.
- Verification:
  - `timeout 50s make TIMED=1 ContextLogic/theories/FormulaFiberwise.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `2.29s`.

## Batch 10: `TypePersistBase.v`

- Baseline from the focused run before this batch: `7.07s`.
- `COQEXTRAFLAGS=-time` showed the largest local hotspot in
  `ty_denote_gas_persist_over_ret_fvar_self_body`:
  - a `cbn [fv_tm fv_value]; set_solver` side condition for a shifted
    `ret` term, costing about `0.82s`;
  - repeated singleton-subset side conditions
    `{[z]} ⊆ world_dom m`, each solved by a broad `set_solver` despite the
    already available `Hzm : z ∈ world_dom m`.
- Optimization:
  - Replaced the shifted-ret FV side condition with a direct
    `not_elem_of_empty` proof after normalization.
  - Replaced all five repeated singleton-subset proofs with explicit
    singleton elimination and `Hzm`.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypePersistBase.vo`
    passed.
  - Focused time after the batch: `5.38s`.
  - Remaining visible hotspots are in the ret-fvar swap/projection proof and
    a basic-world closed-on extraction; those are candidates for a later,
    more semantic helper rather than another tiny side-condition rewrite.

## Batch 11: `TypeDenote.v`

- Baseline focused time before this batch: `5.64s`.
- `COQEXTRAFLAGS=-time` showed the main hotspots in
  `ty_denote_gas_lvars_subset`:
  - the `CTOver` and `CTUnder` cases each spent about `0.78s` in a broad
    `better_set_solver` after lvars normalization;
  - `CTInter` and `CTUnion` still spend about `0.63s` each in broad set
    solving and are left for a later guard-support helper.
- Optimization:
  - Reused the existing local `solve_lvars_element` element-level tactic for
    the `CTOver`/`CTUnder` cases.
  - Replaced the difference-subset side condition passed to
    `lvars_at_depth_mono` by a direct `elem_of_difference` proof.

## Batch 12: `TypePersistSingleton.v`

- Focused time before this batch: `4.08s`.
- `COQEXTRAFLAGS=-time` showed repeated side-condition hotspots in the
  singleton result-slot support:
  - `storeA_restrict_dom` membership proofs for the fresh result slot,
    about `0.10s` each in the pullback/push lemmas;
  - duplicated derivations of `z ∈ world_dom m` and
    `fv_cty τ ⊆ world_dom m` from a singleton restriction;
  - shifted `ret` term FV side conditions solved by broad `set_solver`.
- Optimization:
  - Added a local helper `elem_open_world_inter_singleton` for the recurring
    open-world/singleton membership goal.
  - Replaced repeated `A`-membership and world-domain asserts with direct
    union/singleton reasoning.
  - Reused `res_restrict_singleton_exact_dom_subset` instead of reconstructing
    singleton-domain inclusions twice.
  - Replaced shifted-ret FV side conditions with `not_elem_of_empty` and
    unshifted ret membership with direct singleton proofs.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypePersistSingleton.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `1.43s`.
  - Remaining visible cost is mostly fixed imports plus small domain-equality
    proofs around `storeA_restrict_dom`; no multi-second solver remains.

## Batch 13: `TypeEquivTLetFormula.v`

- Baseline from `CompileTimeStats.txt`: `1.98s`, `8.22 s/KLOC`.
- Focused `COQEXTRAFLAGS=-time` run before the change showed three concrete
  slow side conditions in `result_graph_tlet_reverse_transport`:
  - a base-domain exclusion proof after `res_restrict_dom`, about `0.32s`;
  - an extension-output disjointness proof, about `0.68s`;
  - an `x ∉ FV(D) ∪ FV(e1)` side condition, about `0.60s`.
- Optimization:
  - Replaced the base-domain proof by explicit intersection/difference
    reasoning from `HDm` and `x ∉ D`.
  - Replaced output disjointness by a direct singleton contradiction using
    `Hx_base`.
  - Replaced the final FV/freshness solver with an explicit split over
    `lvars_fv D ∪ fv_tm e1`.
  - Replaced two reflexive domain-subset `ltac:(set_solver)` arguments by
    identity subset proofs.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypeEquivTLetFormula.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `0.45s`.
  - Slightly extended `solve_lvars_element` so it handles both left- and
    right-associated two-level union goals.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypeDenote.vo` passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `3.96s`.

## Batch 14: `SoundnessFixSelf.v`

- Baseline from a clean focused `COQEXTRAFLAGS=-time` run before this batch:
  `9.09s` for 1652 lines, about `5.50 s/KLOC`.
- Concrete hotspots:
  - a freshness side condition for two result-first binders used
    `set_solver` and spent about `2.55s`;
  - a singleton `relevant_lvars` membership proof for the recursive argument
    world also used broad `set_solver` and spent about `2.55s`;
  - a static-guard environment-domain inclusion spent about `3.23s` before
    context clearing.
- Optimization:
  - Replaced the binder-inequality side conditions by direct contradiction
    proofs using the fresh hypothesis and `fix_self_in_union`.
  - Replaced the singleton `relevant_lvars` membership proof by the explicit
    right-union/singleton proof.
  - Cleared the context before the remaining domain-inclusion set proof so it
    solves the actual set expression instead of searching the whole Fix proof
    context.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessFixSelf.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `3.33s`, about `2.02 s/KLOC`.

## Batch 15: `SoundnessLamArrow.v`

- Baseline from the focused `COQEXTRAFLAGS=-time` run before this batch:
  `3.74s` for about 1100 lines, roughly `3.4 s/KLOC`.
- Concrete hotspots:
  - repeated local proofs of
    `lc_lvars (context_ty_lvars_at 1 τ)` rebuilt the same
    `cty_lc_at_lvars_bv_empty` argument and ended with a broad `set_solver`;
  - repeated `ctx_erasure_under_norm_in ...; better_set_solver` proofs for
    `erase_ctx` freshness and relevant-env freshness;
  - direct uses of `relevant_env_arrow_fresh_free` /
    `relevant_env_wand_fresh_free` in the target proof re-ran normalization
    already available through `SoundnessSetMapFacts`.
- Optimization:
  - Added `soundness_lam_lc_lvars_context_ty_lvars_at_of_lc` in
    `SoundnessLamBase.v` and replaced three duplicated local LC proofs in
    Lam base/Arrow/Wand support.
  - Imported `SoundnessSetMapFacts` in `SoundnessLamArrow.v` and replaced
    repeated direct relevant-env freshness derivations with
    `soundness_typed_bind_arrow_value_fresh`,
    `soundness_relevant_env_arrow_value_fresh`,
    `soundness_relevant_env_wand_value_fresh`, and
    `soundness_fresh_erase_ctx_from_context_union`.
  - Replaced one opened-world membership proof by an explicit
    union/singleton construction instead of `set_solver`.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessLamBase.vo`
    passed.
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessLamArrow.vo`
    passed.
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessLamWand.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `2.60s`, about `2.36 s/KLOC`.

## Batch 16: `SoundnessFix.v`

- Baseline from the focused run before this batch: `3.24s` for 681 lines,
  about `4.76 s/KLOC`.
- Concrete hotspots:
  - two outer result-slot relevant-env freshness proofs directly called
    `relevant_env_arrow_fresh_free` with repeated
    `ctx_erasure_under_norm_in ...; better_set_solver`;
  - the result-first Arrow body repeated the same opened-world membership and
    `erase_ctx` freshness side conditions as `SoundnessLamArrow.v`;
  - the final environment-agreement step rebuilt
    `lc_lvars (context_ty_lvars_at 1 τ)` locally.
- Optimization:
  - Imported `SoundnessSetMapFacts` and `SoundnessLamBase` in
    `SoundnessFix.v`.
  - Replaced direct relevant-env freshness derivations with
    `soundness_relevant_env_arrow_value_fresh` and
    `soundness_typed_bind_arrow_value_fresh`.
  - Replaced `erase_ctx` freshness obligations by
    `soundness_fresh_erase_ctx_from_context_union`.
  - Replaced opened-world membership proofs by explicit union/singleton
    construction.
  - Reused `soundness_lam_lc_lvars_context_ty_lvars_at_of_lc` for the
    result-body relevant-lvars side condition.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessFix.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - `timeout 50s make TIMED=1 ContextTyping/theories/Soundness.vo` passed.
  - New focused time observed: `1.77s`, about `2.60 s/KLOC`.

## Batch 17: `SoundnessApp.v`

- Baseline from the focused run before this batch: `3.91s` for 1309 lines,
  about `2.99 s/KLOC`.
- Concrete hotspot:
  - `app_arrow_result_to_target` manually unfolded `relevant_lvars`, rebuilt
    the bound-lvar emptiness proof for `cty_open 0 x τ`, and ended in a broad
    `set_solver`; the timed run charged about `0.286s` to that final solver
    block.
- Optimization:
  - Replaced the whole hand-written `relevant_lvars` proof with the existing
    `lc_lvars_relevant_lvars` helper, using the `context_typing_wf` LC fact for
    the opened type and direct LC for `tapp v1 (vfvar x)`.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessApp.vo` passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `3.67s`, about `2.80 s/KLOC`.

## Batch 18: `SoundnessFixApply.v` result-alias side conditions

- Starting point after earlier FixApply cleanup: focused `COQEXTRAFLAGS=-time`
  run around `5.85s`, with remaining hotspots in opened Fix application side
  conditions and the nested result-alias call.
- Concrete hotspots:
  - repeated proofs that the result slot is fresh for opened body/type syntax
    and for `relevant_lvars τres (tapp_tm (tret body) self)`;
  - an embedded `set_solver` in the `ty_denote_gas_tapp_fun_arg_result_alias`
    call proving inserted-domain membership for `w`, which retimed at about
    `1.37s` when exposed as a named assertion.
- Optimization:
  - Added small local helpers for opened value/type freshness, fix-rec-call
    type freshness, recursive self freshness, and result-application
    `relevant_lvars` freshness.
  - Derived the `z` and `w` freshness facts once and reused them through the
    result-first open/result alias proof.
  - Moved the alias theorem side conditions out of embedded `ltac:(...)`
    arguments and proved the inserted-domain memberships directly by
    union/singleton constructors.
  - Tried replacing the context-erasure domain inclusion with a shorter
    domain-only proof, but it timed out; reverted that part and left it as a
    deferred lower-level lemma candidate.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessFixApply.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed before the reverted domain
    experiment; new focused time observed: `4.01s`.

## Batch 19: `SoundnessPersist.v` persist-intro side conditions

- Baseline from focused `COQEXTRAFLAGS=-time` runs before this batch:
  `SoundnessPersist.vo` around `4.00s` for roughly 676 lines, about
  `5.9 s/KLOC`.
- Concrete hotspots:
  - three `wfworld_closed_on_le` side conditions in
    `ty_denote_gas_persist_ret_value_intro_singleton` repeatedly solved
    `fv_cty τ` / `fv_value v` subset goals from the large `HA_sub` context;
  - embedded `ltac:(set_solver)` arguments to
    `res_restrict_singleton_push_ret_value_result`;
  - the final `res_models_persist_intro_from_singleton_superset` support
    proved a simple `ty_denote_gas_fv_subset` target with a broad
    `set_solver`, costing about `0.43s`;
  - inside `res_restrict_singleton_push_ret_value_result`, the proof
    `a ∈ A ∪ {[y]}, a <> y -> a ∈ A` cost about `0.11s`.
- Optimization:
  - Named the `fv_cty τ` and `fv_value v` inclusions into `m` and `my`
    once (`Hτ_world`, `Hv_world`, `Hτ_my`, `Hv_my`) and reused them in the
    closedness proofs.
  - Added explicit small facts `HvA` and `HyA` for the result-slot singleton
    helper call, replacing embedded `ltac` arguments.
  - Replaced the final `ty_denote_gas_fv_subset` support solver with a direct
    two-branch union proof.
  - Replaced the `a ∈ A` proof in the store-map equality block with direct
    union/singleton destructuring.
- Verification:
  - `timeout 50s make TIMED=1 ContextTyping/theories/SoundnessPersist.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - `timeout 50s make TIMED=1 ContextTyping/theories/Soundness.vo` passed.
  - New focused time observed: `2.04s`, about `3.0 s/KLOC`.
- Deferred:
  - Remaining time is concentrated in the lower ret-value/result-slot helper,
    especially exact result formula extraction and two y-domain store lookup
    proofs. These should be treated as a separate lower-level lemma/facts
    batch rather than folded into this persist-intro cleanup.

## Batch 20: `TypeEquivTermTLet.v` tlet fiber-extension side conditions

- Baseline from `CompileTimeStats.txt`:
  `Denotation/theories/TypeEquivTermTLet.v` was about `7.64s` for 369 lines,
  roughly `20.7 s/KLOC`.
- Focused baseline before this batch:
  `COQEXTRAFLAGS=-time` rebuilt the file in about `3.69s`, with hotspots in
  `tm_fiber_equiv_tlete_body_extension`:
  repeated fresh-for decomposition, a broad `try set_solver` in
  `tm_eval_in_store_tlete_intro_closed_on`, and several store/domain
  side conditions around extension output singletons.
- Optimization:
  - Added `tlet_fresh_for_parts` to split the common `fresh_for` union once
    outside the large proof context.
  - Replaced the `tlete` intro `try set_solver` block with explicit premises.
  - Replaced two extension input-domain proofs with `extA_projection_dom`
    instead of `res_extend_by_input_dom` plus broad set solving.
  - Replaced singleton-output disjointness and output lookup side conditions
    with `disjoint_singleton_l` and direct domain facts.
- Verification:
  - `timeout 50s make TIMED=1 Denotation/theories/TypeEquivTermTLet.vo`
    passed.
  - Focused `COQEXTRAFLAGS=-time` run passed.
  - New focused time observed: `0.69s`.
- Deferred:
  - The remaining per-KLOC number is mostly import/fixed overhead for a very
    small file; no proof command above the import line remained near the old
    hotspots.
