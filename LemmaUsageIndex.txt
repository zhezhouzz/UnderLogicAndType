Lemma usage index, refreshed top-down from Denotation.

Method:
- Counted Rocq declarations starting with Lemma/Theorem/Corollary/Fact/Proposition.
- Usage count is whole-token `rg` over Rocq source files.
- "zero-use" means the declaration name appears only at its definition site.
- "single-use" means the declaration plus one real source use.

Current summary:
- Denotation/theories: 127 declarations, 0 zero-use, 65 single-use.
- ContextBasicDenotation/theories: 244 declarations, 0 zero-use, 119 single-use.
- ContextTypeLanguage/theories: 92 declarations, 0 zero-use, 35 single-use.
- ContextLogic/theories: 146 declarations, 0 zero-use, 51 single-use.
- ContextAlgebra/theories: 120 declarations, 0 zero-use, 65 single-use.
- Lower library layer (ContextStore, ContextBase, CoreLang, ContextTyping): 755 declarations, 208 zero-use, 224 single-use.

Cleanup performed in this pass:
- Denotation: inlined and removed the two sum target-zero wrappers in TypeEquivBody.
- ContextTypeLanguage: removed unused qualifier/type-lvar msubst and freshness helpers.
- ContextLogic: removed unused formula/open and lstore restrict helpers from the earlier pass.
- ContextAlgebra: removed old fiberwise/msubst support tails, including singleton pullback, nested fiber union, input-widen extension commute, and stale interface wrappers.

Judgment calls:
- The remaining lower-layer zero-use declarations are kept as foundational syntax, store, open, locally-nameless, operational, and typing-library facts. They are not old fiberwise-route glue, and deleting them would make the base proof library harder to search/reuse.
- Interface files are not treated as public surfaces. Their unused wrappers were removed when they only mirrored unused internal facts.
- Single-use Denotation lemmas were kept when they mark semantic boundaries, induction packages, or compile-time proof structure.

Known long proof retained:
- ContextLogic/theories/FormulaSemantics.v: res_models_fuel_projection remains slightly over 200 lines. A broader projection solver was tested and made proof checking slower, so the explicit resource-case projection script is intentionally retained.

Alias / notation cleanup:
- Removed ContextAlgebra/theories/ResourceSurface.v and its _CoqProject entry. It was an unused surface-only wrapper with smoke examples and no source imports.
- Removed notation smoke modules from CoreLang/theories/SyntaxNotation.v, ContextLogic/theories/FormulaNotation.v, and ContextTypeLanguage/theories/Notation.v. The actual notations remain.
- Removed the duplicate local/global `F ~>i F'` notation by spelling out the local helper premise and keeping the single global notation.
- Removed the parsing-only `ty_denote_ctx` alias and unfolded its three source uses to `ty_denote (erase_ctx Γ) ...`.
- Kept `ty_denote_under` for now: although it ignores ambient Σ, it is the active direct Soundness target shorthand rather than an unused parsing alias.

FBWand quality-refactor pass:
- This pass intentionally did not delete genuinely unused foundational or semantic lemmas.
- Extracted the comma/star context-erasure union equalities from the corresponding
  `basic_world_formula` lemmas in Denotation/theories/Context.v, so the
  basic-world lemmas are now short semantic wrappers over a named equality.
- Removed two unused wrapper lemmas from ContextStore/StoreRestrict.v:
  `storeA_union_absorb_restrict_inside_r` and
  `storeA_union_absorb_prefix_inside_r`.  The lower, reusable absorb lemmas
  remain.
- Current declaration counts by directory after this pass:
  Denotation/theories: 152
  ContextBasicDenotation/theories: 268
  ContextTypeLanguage/theories: 98
  ContextLogic/theories: 156
  ContextAlgebra/theories: 137
  ContextStore: 249
  ContextBase: 203
  CoreLang/theories: 274
  ContextTyping/theories: 77
