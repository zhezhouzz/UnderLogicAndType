# Type Denote Recovery Status

## Current Shape

- `ty_denote_gas` now uses the original denotation environment `Σ`.
- `CTOver`, `CTUnder`, and `CTSum` use result graphs of the form:
  `expr_result_formula_at (dom Σ) ...`.
- `expr_result_formula_at` takes an `lvset`; the old atom-set wrapper
  `expr_result_formula_on X` remains only as `lvars_of_atoms X`.
- Fiber transport naming is back to `equiv`, not `refines`.
- `typed_fiber_equiv_on` observes `fv_cty τ`; the equality is exact on that
  fixed fiber, while result-graph fibering is controlled by `dom Σ` inside the
  type denotation.

## Recovered

- `make TIMED=1` completes.
- `ContextTyping/theories/Soundness.vo` compiles.
- Old theorem names containing `typed_fiber_refines_set`,
  `typed_fiber_refines_on`, `tm_fiber_refines_on`, and
  `ty_denote_gas_tm_fiber_refine` have been removed from active proof code.

## Temporary Named Admits

Status: closed.  The repository currently has no `Admitted.` or `admit.`
occurrences.

Historical note: this file originally tracked the recovery period where
fiber/result transport, result alias/extension, TLet, Soundness cases, and the
concrete primitive context were temporarily admitted.  Those obligations have
since been proved or reorganized into real proof files.

## Next Proof Direction

The next focused proof pass should not restart from the old admitted skeletons.
For optimization, use the current proved route:

- ordinary term equivalence remains a compatibility route;
- fiber result-set equality and projected result correspondence are the
  nondeterministic-ready route;
- result-first Arrow/Wand and TLet helpers are the current public bridge.
